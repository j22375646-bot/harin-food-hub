'use client';

import {useState} from 'react';
import HarinIcon from './_design-system/harin-icon.js';

const LABELS={AWS_SYSTEMD:'AWS systemd',VERCEL_CRON:'Vercel Cron',SUPABASE_CRON:'Supabase Cron',SUPABASE_TABLE:'Supabase 테이블',MANUAL:'사장님 수동',VERCEL_FUNCTION:'Vercel 함수',AWS_FIXED_IP_WORKER:'AWS 고정 IP 워커',SUPABASE_DATABASE:'Supabase DB',OWNER:'사장님',SUPABASE_CUSTOM_TABLE:'사용자 정의 큐',NONE:'직접 실행'};
const STATES={ACTIVE:['운영 신호 확인','ready'],OBSERVE:['관찰 중','observe'],CHECK:['신호 확인 필요','check'],COLLISION_RISK:['중복 위험','danger'],BLOCKED:['전환 차단','danger']};
const KST=new Intl.DateTimeFormat('ko-KR',{timeZone:'Asia/Seoul',month:'numeric',day:'numeric',hour:'numeric',minute:'2-digit'});
function date(value){return value?KST.format(new Date(value)):'아직 없음';}
function State({value}){const [label,tone]=STATES[value]||['확인 필요','check'];return <span className={`executionPathState ${tone}`}><i/>{label}</span>;}
function FlowStep({icon,label,value}){return <span className="executionFlowStep"><i><HarinIcon name={icon} size={20}/></i><small>{label}</small><b>{LABELS[value]||value}</b></span>;}
function Lane({lane}){return <article className={`executionLane ${String(lane.state||'').toLowerCase()}`}><header><span className="executionLaneIcon"><HarinIcon name={lane.current_executor==='AWS_FIXED_IP_WORKER'?'server':lane.current_trigger.includes('CRON')?'clock':'collection'} size={23}/></span><div><small>{lane.lane_key}</small><h2>{lane.label}</h2><p>{lane.schedule_label}</p></div><State value={lane.state}/></header><div className="executionFlow"><FlowStep icon="clock" label="시작" value={lane.current_trigger}/><HarinIcon name="chevron" size={18}/><FlowStep icon="database" label="대기·전달" value={lane.queue_backend}/><HarinIcon name="chevron" size={18}/><FlowStep icon="server" label="실행" value={lane.current_executor}/></div><div className={`executionAutoGuard ${String(lane.protectionState||'').toLowerCase()}`}><HarinIcon name={lane.protectionState==='PROTECTED'?'check':'warning'} size={18}/><span><b>{lane.protectionState==='PROTECTED'?'단일 실행 경로 확인':'자동 확인 필요'}</b><small>{lane.guardLabel||lane.ownerKey}{lane.recoveryCount?` · 임대 복구 ${lane.recoveryCount}회`:''}</small></span></div><footer><span><small>실제 경로</small><b>{lane.source_path}</b></span><span><small>최근 근거</small><b>{lane.evidenceLabel}</b><em>{date(lane.lastEvidenceAt)}</em></span></footer></article>;}

function HandoverPanel({handover={}}){
  const ready=handover.status==='READY';
  return <section className={`executionHandover ${String(handover.status||'WAIT').toLowerCase()}`}><header><span><HarinIcon name={ready?'check':'shield'} size={25}/></span><div><small>PHASE 21-7 · OWNERSHIP EVIDENCE</small><b>전환 결과 대조·소유권 인수 조건</b><p>{handover.message||'현재 실행 경로를 유지하며 인수 조건을 확인합니다.'}</p></div><aside><em>소유권 변경</em><strong>{handover.ownershipChanges||0}건</strong><small>스냅샷 {handover.snapshotHash||'-'}</small></aside></header><ul>{(handover.checks||[]).map(check=><li className={check.ready?'ready':'wait'} key={check.key}><HarinIcon name={check.ready?'check':'clock'} size={17}/><span>{check.label}</span><b>{check.ready?'확인':'대기'}</b></li>)}</ul></section>;
}

function CredentialChecklist({checklist={}}){
  return <section className="executionCredentials"><header><span><HarinIcon name="key" size={25}/></span><div><small>PHASE 21-8 · DEFERRED CREDENTIALS</small><b>나중에 한 번에 입력할 API 준비표</b><p>{checklist.entryWindow||'개발 검증 뒤 한 번에 입력합니다.'} · 키 값은 화면에 표시하지 않습니다.</p></div><aside><strong>{checklist.ready||0}/{checklist.total||0}</strong><small>항목 준비 · 미입력 {checklist.missing||0}개</small></aside></header><div className="executionCredentialGroups">{(checklist.groups||[]).map(group=><details key={group.key}><summary><span><i><HarinIcon name={group.icon||'key'} size={20}/></i><b>{group.label}</b><small>{group.destination}</small></span><em className={group.status==='READY'?'ready':group.status==='PARTIAL'?'partial':'setup'}>{group.status==='READY'?'준비됨':group.status==='PARTIAL'?`${group.ready}/${group.total}`:'나중에 입력'}</em></summary><p>{group.description}</p><ul>{group.fields.map(field=><li key={field.name}><span><code>{field.name}</code>{field.alternatives?.length?<small>또는 {field.alternatives.join(', ')}</small>:null}</span><em>{field.configured?'설정됨':'미입력'}</em></li>)}</ul></details>)}</div><footer>{(checklist.rules||[]).map(rule=><span key={rule}><HarinIcon name="check" size={15}/>{rule}</span>)}</footer></section>;
}

export default function ExecutionPathCenter({center:initialCenter={}}){
  const [center,setCenter]=useState(initialCenter);
  const [checking,setChecking]=useState(false);
  const [message,setMessage]=useState('');
  const summary=center.summary||{},worker=center.worker||{},handover=center.handover||{},credentialChecklist=center.credentialChecklist||{};
  async function checkNow(){
    if(checking)return;
    setChecking(true);setMessage('현재 실행 경로를 다시 확인하고 있어요…');
    try{
      const response=await fetch('/api/infrastructure/execution-paths/check',{method:'POST'});
      const result=await response.json();
      if(!response.ok||!result.ok)throw new Error(result.error||'확인 실패');
      setCenter(result.center);
      const next=result.center.summary||{};
      const dry=result.center.dryRun||{};
      const handoverResult=result.center.handover||{};
      setMessage(next.collisionKeys?`중복 위험 ${next.collisionKeys}건을 발견했어요.`:handoverResult.status==='READY'?`최종 대조 통과 · 소유권은 바꾸지 않고 ${handoverResult.totalChecks||0}개 인수 조건을 확인했어요.`:next.activeQueue?`중복은 없어요 · 진행 중인 작업 ${next.activeQueue}건이 끝난 뒤 인수할 수 있어요.`:dry.status==='PASS'?`드라이런 통과 · ${dry.guardedLanes||0}개 경로가 한 번만 실행되도록 보호돼요.`:`드라이런 확인 필요 · ${(dry.blockers||[]).join(' · ')}`);
    }catch(error){setMessage(`확인 필요 · ${error.message}`);}finally{setChecking(false);}
  }
  const dry=center.dryRun||{};
  const recovery=center.recovery||{};
  return <section className="executionPathCenter"><header className="executionPathHero"><div className="executionPathHeroIcon"><HarinIcon name="server" size={31}/></div><div><span>PHASE {center.phase||'21-7 · 21-8'} · FINAL READINESS</span><h1>작업 실행 경로·전환센터</h1><p>현재 실행 경로를 안전하게 대조하고, 마지막에 입력할 외부 API 준비 항목까지 한곳에서 확인합니다.</p></div><aside><b>검수·키 준비</b><small>소유권 변경 0건 · 추가 잠금 없음</small></aside></header><section className="executionPathGuard"><HarinIcon name="shield" size={23}/><span><b>중복과 인수 조건을 한 번에 확인해요</b><small>현재 경로를 끄지 않고 조건을 검사하며, 시간별·일일 수집과 예약 작업은 완료 결과를 재사용해 중복 실행을 막습니다.</small></span><button type="button" onClick={checkNow} disabled={checking}><HarinIcon name="sync" size={19}/>{checking?'확인 중…':'최종 상태 다시 확인'}</button></section>{message?<p className={`executionCheckMessage ${summary.collisionKeys||dry.status==='BLOCKED'?'danger':'ready'}`} role="status">{message}</p>:null}<section className={`executionDryRun ${dry.status==='BLOCKED'?'blocked':'ready'}`}><span><HarinIcon name={dry.status==='PASS'?'check':'warning'} size={23}/><b>21-3 전환 드라이런</b><small>{dry.status==='PASS'?'운영 경로 변경 없이 통과했어요.':'조건을 확인해야 해요.'}</small></span><ol>{(dry.sequence||[]).map((step,index)=><li key={step}><i>{index+1}</i>{step}</li>)}</ol></section><section className={`executionRecovery ${recovery.status==='CHECK'?'check':'ready'}`}><header><HarinIcon name={recovery.status==='CHECK'?'warning':'sync'} size={24}/><span><b>21-6 자동 복구 상태</b><small>{recovery.status==='CHECK'?'최근 실패를 확인해주세요. 이전 성공 자료는 유지 중입니다.':'최근 실패 없이 단일 실행 임대가 정상이에요.'}</small></span><em>복구 {summary.recoveredRuns||0}회 · 실패 {summary.failedRuns||0}건</em></header><ul>{(recovery.rules||[]).map(rule=><li key={rule}><HarinIcon name="check" size={15}/>{rule}</li>)}</ul></section><HandoverPanel handover={handover}/><section className="executionPathKpis"><article><HarinIcon name="link" size={22}/><span><small>자동 보호 경로</small><b>{summary.protectedLanes||0}/{summary.lanes||0}개</b></span></article><article><HarinIcon name="checklist" size={22}/><span><small>인수 조건</small><b>{handover.readyChecks||0}/{handover.totalChecks||0}개</b></span></article><article className={summary.collisionKeys?'danger':''}><HarinIcon name="warning" size={22}/><span><small>활성 멱등키 충돌</small><b>{summary.collisionKeys||0}건</b></span></article><article><HarinIcon name="server" size={22}/><span><small>고정 IP 워커</small><b>{worker.ready?'정상':'확인 필요'}</b><em>{worker.lastSeenAt?`${worker.ageMinutes}분 전 · ${worker.sourceIp||'IP 확인'}`:'신호 없음'}</em></span></article></section><section className="executionQueueTruth"><span><HarinIcon name="database" size={22}/><b>현재 큐</b><small>Supabase 사용자 정의 테이블 · 활성 {summary.activeQueue||0}건</small></span><span className={summary.nativeQueueEnabled?'ready':'muted'}><HarinIcon name="collection" size={22}/><b>Supabase 네이티브 Queue</b><small>{summary.nativeQueueEnabled?'사용 확인됨':'현재 미사용 · 기존 큐 유지'}</small></span></section><div className="executionLaneGrid">{(center.lanes||[]).map(lane=><Lane lane={lane} key={lane.lane_key}/>)}</div>{center.collisions?.length?<section className="executionCollision" role="alert"><HarinIcon name="warning" size={24}/><div><b>중복 실행 위험을 발견했어요</b>{center.collisions.map(item=><p key={`${item.source}:${item.key}`}>{item.source} · {item.key} · 활성 {item.count}건</p>)}</div></section>:null}<CredentialChecklist checklist={credentialChecklist}/><details className="naverApiHelp executionPathHelp"><summary><span><HarinIcon name="shield" size={20}/><b>자동 보호 기준</b></span><em>열기</em></summary><ol>{(center.protections||[]).map(rule=><li key={rule}>{rule}</li>)}</ol><p>과거 멱등키 없는 기록 {summary.legacyWithoutKey||0}건은 현재 활성 작업으로 세지 않고 참고 이력으로만 남깁니다.</p></details></section>;
}
