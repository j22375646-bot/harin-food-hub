import {HarinRouteSkeleton} from '../_design-system/harin-loading-screen.js';

export default function Phase28Loading() {
  return <HarinRouteSkeleton
    title="오늘 운영 화면을 먼저 열고 있어요"
    description="공통 메뉴를 바로 보여드리고 매출·주문 자료는 준비되는 순서대로 연결합니다."
  />;
}
