'use client';

import { useState } from 'react';
import { useStoredState } from './use-hub-preference.js';

const count=value=>Number(value||0).toLocaleString('ko-KR');
const dateTime=value=>value?new Intl.DateTimeFormat('ko-KR',{timeZone:'Asia/Seoul',year:'numeric',month:'numeric',day:'numeric',hour:'numeric',minute:'2-digit'}).format(new Date(value)):'시각 없음';
const platformLabel={NAVER:'네이버',COUPANG:'쿠팡',CAFE24:'Cafe24'};
const kindLabel={INQUIRY:'문의',CANCEL:'취소',RETURN:'반품',EXCHANGE:'교환'};
const couriers=[['EPOST','우체국택배'],['CJGLS','CJ대한통운'],['HANJIN','한진택배'],['LOTTE','롯데택배'],['LOGEN','로젠택배']];

async function fixedIpResult(response){const initial=await response.json();if(response.status!==202||!initial.request?.id)return initial;for(let attempt=0;attempt<90;attempt+=1){await new Promise(resolve=>setTimeout(resolve,750));const statusResponse=await fetch(`/api/coupang/operations/${initial.request.id}`,{cache:'no-store'});const result=await statusResponse.json();if(statusResponse.status===202)continue;return result;}return {ok:false,error:'서울 고정 IP 서버의 응답 시간이 초과됐습니다. 처리기록에서 상태를 확인해주세요.'};}

function ChannelState({item}){return <article className={`unifiedCsChannel ${String(item.status||'SETUP_REQUIRED').toLowerCase()}`}><i aria-hidden="true"/><div><b>{item.label}</b><small>{item.message}</small></div><em>{item.statusLabel}</em></article>;}

function LinkedOrder({row}){const order=row.order;if(!row.orderId)return <div className="unifiedCsNoOrder">주문번호가 없는 상품 문의입니다.</div>;if(!order)return <div className="unifiedCsNoOrder">주문 {row.orderId} · 주문 상세 연결 대기</div>;return <section className="unifiedCsOrderLink"><header><span>연결된 주문</span><b>{order.orderId}</b><em>{order.status||'상태 확인 필요'}</em></header><div><span><small>주문시각</small><b>{dateTime(order.orderedAt)}</b></span>{order.shipmentBoxId&&<span><small>배송번호</small><b>{order.shipmentBoxId}</b></span>}<span><small>주문금액</small><b>{Number(order.amount||0).toLocaleString('ko-KR')}원</b></span></div>{(order.products||[]).length?<ul>{order.products.slice(0,4).map((product,index)=><li key={`${product.name}-${index}`}><span><b>{product.name}</b>{product.option&&<small>{product.option}</small>}</span><strong>{count(product.quantity)}개</strong></li>)}</ul>:<small>상품 상세 연결 대기</small>}</section>;}

function AuditTrail({audit}){if(!audit)return <small className="unifiedCsAudit emptyAudit">아직 실행한 처리기록이 없습니다.</small>;return <small className={`unifiedCsAudit ${String(audit.status||'').toLowerCase()}`}><b>최근 처리기록</b> · {audit.operationType} · {audit.status} · {dateTime(audit.executedAt||audit.requestedAt)}{audit.errorMessage?` · ${audit.errorMessage}`:''}</small>;}

function InquiryCard({row,templates}){const item=row.source||{};const [replyBy,setReplyBy]=useStoredState('cs:reply-by','');const [content,setContent]=useState('');const [working,setWorking]=useState(false);const [message,setMessage]=useState('');const [template,setTemplate]=useState('');function applyTemplate(id){setTemplate(id);const selected=(templates||[]).find(entry=>entry.id===id);if(selected)setContent(selected.content);}async function submit(){const action=item.inquiry_type==='CALL_CENTER'?'REPLY_CALL_CENTER':'REPLY_ONLINE';if(!window.confirm(`문의 ${item.inquiry_id}에 아래 답변을 실제 전송합니다.\n\n${content}\n\n전송할까요?`))return;setWorking(true);setMessage('서울 고정 IP 서버에서 전송 중…');try{const response=await fetch('/api/coupang/cs/action',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({confirm:true,action,inquiryId:item.inquiry_id,replyBy,content,parentAnswerId:item.parent_answer_id})});const result=await fixedIpResult(response);if(!result.ok)throw new Error(result.error||'전송 실패');setMessage('답변 전송 완료 · 처리기록에 저장됐습니다.');setContent('');setTemplate('');}catch(error){setMessage(`전송 실패 · ${error.message}`);}finally{setWorking(false);}}return <article className={`unifiedCsWorkCard inquiry ${row.due.code.toLowerCase()}`}><header><div><span className={`platformPill ${row.platform.toLowerCase()}`}>{platformLabel[row.platform]}</span><span className="kindPill">{row.kindLabel}</span><span className={`duePill ${row.due.code.toLowerCase()}`}>{row.due.label}</span></div><strong>문의 {row.sourceId}</strong></header><div className="unifiedCsQuestion"><small>{dateTime(row.occurredAt)}{row.due.ageHours!=null?` · ${count(row.due.ageHours)}시간 경과`:''}</small><p>{row.content}</p></div><LinkedOrder row={row}/>{!row.completed&&<section className="unifiedCsReply"><div><label><span>답변 템플릿</span><select value={template} onChange={event=>applyTemplate(event.target.value)}><option value="">직접 작성</option>{(templates||[]).map(entry=><option value={entry.id} key={entry.id}>{entry.label}</option>)}</select></label><label><span>쿠팡 Wing 사용자 ID</span><input value={replyBy} onChange={event=>setReplyBy(event.target.value)} placeholder="답변 전송 계정"/></label></div><textarea rows="5" maxLength="1000" value={content} onChange={event=>setContent(event.target.value)} placeholder="고객에게 보낼 답변을 확인한 뒤 전송하세요."/><footer><small>템플릿은 초안입니다. 내용 확인 후에만 실제 전송됩니다.</small><button disabled={working||!replyBy||content.trim().length<2} onClick={submit}>{working?'전송 중…':'내용 확인 후 답변 전송'}</button></footer></section>}{message&&<p className="unifiedCsMessage">{message}</p>}<AuditTrail audit={row.audit}/></article>;}

function ClaimCard({row}){const item=row.source||{};const isReturn=row.kind!=='EXCHANGE';const [working,setWorking]=useState(false),[message,setMessage]=useState('');const [courier,setCourier]=useState('EPOST'),[invoice,setInvoice]=useState(''),[rejectCode,setRejectCode]=useState('SOLDOUT');async function run(action){const labels={RETURN_RECEIVE:'반품상품 입고 확인',RETURN_APPROVE:'반품 승인·환불',RETURN_PICKUP_INVOICE:'반품 회수송장 등록',EXCHANGE_PICKUP_INVOICE:'교환 회수송장 등록',EXCHANGE_RECEIVE:'교환상품 입고 확인',EXCHANGE_REJECT:'교환 거부',EXCHANGE_SHIPPING_INVOICE:'교환상품 출고송장 등록'};if(!window.confirm(`${labels[action]}을 실제 쿠팡에 반영합니다.\n주문 ${row.orderId||'-'} · 접수 ${row.sourceId}\n\n계속할까요?`))return;setWorking(true);setMessage('서울 고정 IP 서버에서 처리 중…');try{const response=await fetch('/api/coupang/cases/action',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({confirm:true,action,receiptId:isReturn?row.sourceId:undefined,exchangeId:isReturn?undefined:row.sourceId,deliveryCompanyCode:courier,invoiceNumber:invoice,exchangeRejectCode:rejectCode,shipmentBoxId:item.shipment_box_id})});const result=await fixedIpResult(response);if(!result.ok)throw new Error(result.error||'처리 실패');setMessage('처리 완료 · 감사기록에 저장됐으며 다음 수집에서 상태가 갱신됩니다.');setInvoice('');}catch(error){setMessage(`처리 실패 · ${error.message}`);}finally{setWorking(false);}}const pickupAction=isReturn?'RETURN_PICKUP_INVOICE':'EXCHANGE_PICKUP_INVOICE';return <article className={`unifiedCsWorkCard claim ${row.kind.toLowerCase()} ${row.due.code.toLowerCase()}`}><header><div><span className={`platformPill ${row.platform.toLowerCase()}`}>{platformLabel[row.platform]}</span><span className="kindPill">{kindLabel[row.kind]}</span><span className={`duePill ${row.due.code.toLowerCase()}`}>{row.due.label}</span></div><strong>접수 {row.sourceId}</strong></header><div className="unifiedCsQuestion"><small>{dateTime(row.occurredAt)} · 상태 {row.status}</small><p>{row.content}</p>{item.collect_status&&<em>회수 상태 · {item.collect_status}</em>}</div><LinkedOrder row={row}/>{!row.completed&&<section className="unifiedCsClaimActions"><div>{item.can_receive&&<button onClick={()=>run(isReturn?'RETURN_RECEIVE':'EXCHANGE_RECEIVE')} disabled={working}>입고 확인</button>}{item.can_approve&&<button className="danger" onClick={()=>run('RETURN_APPROVE')} disabled={working}>반품 승인·환불</button>}{item.can_reject&&<><select value={rejectCode} onChange={event=>setRejectCode(event.target.value)}><option value="SOLDOUT">교환상품 품절</option><option value="WITHDRAW">고객 철회</option></select><button className="danger" onClick={()=>run('EXCHANGE_REJECT')} disabled={working}>교환 거부</button></>}</div>{(item.can_pickup_invoice||item.can_shipping_invoice)&&<div><select value={courier} onChange={event=>setCourier(event.target.value)}>{couriers.map(([code,name])=><option value={code} key={code}>{name}</option>)}</select><input inputMode="numeric" value={invoice} onChange={event=>setInvoice(event.target.value.replace(/\D/g,''))} placeholder="송장번호"/>{item.can_pickup_invoice&&<button disabled={working||invoice.length<6} onClick={()=>run(pickupAction)}>회수송장 등록</button>}{item.can_shipping_invoice&&<button disabled={working||invoice.length<6} onClick={()=>run('EXCHANGE_SHIPPING_INVOICE')}>교환 출고송장</button>}</div>}{!item.can_receive&&!item.can_approve&&!item.can_reject&&!item.can_pickup_invoice&&!item.can_shipping_invoice&&<small>현재 상태에서는 직접 처리할 작업이 없습니다.</small>}</section>}{message&&<p className="unifiedCsMessage">{message}</p>}<AuditTrail audit={row.audit}/></article>;}

export default function UnifiedCustomerServiceCenter({center}){const data=center||{rows:[],active:[],channelStates:[],templates:[],summary:{}};const [platform,setPlatform]=useStoredState('filter:cs-platform','ALL',['ALL','NAVER','COUPANG','CAFE24']);const [kind,setKind]=useStoredState('filter:cs-kind','ALL',['ALL','INQUIRY','CANCEL','RETURN','EXCHANGE']);const [due,setDue]=useStoredState('filter:cs-due','ALL',['ALL','TODAY','OVERDUE','WAITING','COMPLETED']);const [query,setQuery]=useStoredState('filter:cs-query','');const [showCompleted,setShowCompleted]=useStoredState('filter:cs-completed',false);const rows=(data.rows||[]).filter(row=>{if(!showCompleted&&row.completed)return false;if(platform!=='ALL'&&row.platform!==platform)return false;if(kind!=='ALL'&&row.kind!==kind)return false;if(due!=='ALL'&&row.due.code!==due)return false;if(query&&!`${row.sourceId} ${row.orderId||''} ${row.title||''} ${row.content||''} ${(row.order?.products||[]).map(item=>`${item.name} ${item.option||''}`).join(' ')}`.toLowerCase().includes(query.toLowerCase()))return false;return true;});const kindTabs=[['ALL','전체'],['INQUIRY','문의'],['CANCEL','취소'],['RETURN','반품'],['EXCHANGE','교환']];return <section className="unifiedCsCenter"><section className="unifiedCsHero"><div><span>PHASE 11-4 · CUSTOMER OPERATIONS</span><h1>통합 CS·클레임센터</h1><p>문의와 취소·반품·교환을 처리기한, 주문, 상품, 배송상태와 함께 확인합니다.</p></div><aside><small>지금 처리할 항목</small><b>{count(data.summary?.active)}건</b><em>기한 초과 {count(data.summary?.overdue)}건</em></aside></section><div className="unifiedCsChannels">{(data.channelStates||[]).map(item=><ChannelState item={item} key={item.platform}/>)}</div><details className="unifiedCsHelp"><summary><span><b>이 화면은 어떻게 쓰나요?</b><small>처리 순서와 표시 기준을 쉬운 말로 확인하세요.</small></span><em>도움말 열기</em></summary><div><p><b>1. 기한 초과부터</b> 접수 후 24시간이 지난 미처리 문의를 먼저 확인합니다.</p><p><b>2. 주문 연결 확인</b> 문의 아래에서 상품과 배송상태를 보고 답변합니다.</p><p><b>3. 답변·처리 확인</b> 템플릿은 초안이며 확인창을 통과해야 실제 전송됩니다.</p><p><b>4. 기록 확인</b> 처리 결과는 최근 처리기록에서 성공·실패와 시각을 확인합니다.</p></div></details><section className="kpiGrid"><article className="kpi orange"><div className="kpiIcon">!</div><div><p>기한 초과</p><strong>{count(data.summary?.overdue)}건</strong><span>가장 먼저 확인</span></div></article><article className="kpi blue"><div className="kpiIcon">Q</div><div><p>미답변 문의</p><strong>{count(data.summary?.unanswered)}건</strong><span>답변 필요</span></div></article><article className="kpi purple"><div className="kpiIcon">R</div><div><p>취소·반품·교환</p><strong>{count(data.summary?.claims)}건</strong><span>진행 단계 확인</span></div></article><article className="kpi green"><div className="kpiIcon">✓</div><div><p>주문 연결</p><strong>{count(data.summary?.linkedOrders)}건</strong><span>상품·배송 함께 확인</span></div></article></section><nav className="unifiedCsKindTabs" aria-label="CS 유형">{kindTabs.map(([id,label])=><button className={kind===id?'active':''} onClick={()=>setKind(id)} key={id}><b>{label}</b><small>{count((data.rows||[]).filter(row=>(id==='ALL'||row.kind===id)&&(!row.completed||showCompleted)).length)}건</small></button>)}</nav><section className="unifiedCsToolbar"><label><span>채널</span><select value={platform} onChange={event=>setPlatform(event.target.value)}><option value="ALL">전체 채널</option><option value="NAVER">네이버</option><option value="COUPANG">쿠팡</option><option value="CAFE24">Cafe24</option></select></label><label><span>처리기한</span><select value={due} onChange={event=>setDue(event.target.value)}><option value="ALL">전체 상태</option><option value="OVERDUE">기한 초과</option><option value="TODAY">오늘 처리</option><option value="WAITING">답변 대기</option><option value="COMPLETED">처리완료</option></select></label><label className="search"><span>검색</span><input type="search" value={query} onChange={event=>setQuery(event.target.value)} placeholder="접수·주문번호, 상품명, 문의 내용"/></label><label className="completed"><input type="checkbox" checked={showCompleted} onChange={event=>setShowCompleted(event.target.checked)}/><span>처리완료도 보기</span></label></section><div className="unifiedCsWorkList">{rows.length?rows.map(row=>row.kind==='INQUIRY'?<InquiryCard row={row} templates={data.templates} key={row.id}/>:<ClaimCard row={row} key={row.id}/>):<div className="unifiedCsEmpty"><b>이 조건에서 처리할 항목이 없습니다.</b><span>연결되지 않은 채널은 위 상태 카드에서 확인할 수 있습니다.</span></div>}</div></section>;}
