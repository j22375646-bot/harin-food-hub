# P4-87 — 캘린더 상태 탐색 / 0.75.0

## 변경
- 전체·진행 중·완료 필터로 월간 날짜별 건수와 선택 날짜 목록을 함께 바꾼다. 조회된 범위의 건수이며 여러 날 일정은 각 날짜에 표시됨을 안내한다.
- 날짜/요일 제목, 일정 상태·유형, 44px 필터와 월 이동 버튼, 14px 정보, 전체 테두리 선택 표시를 적용했다. 작은 창에서는 일정 목록을 달력 아래로 배치한다.
- 날짜 선택/필터 변경/같은 달의 오늘 이동은 받은 자료를 재사용한다. 다른 달 이동과 새로 조회는 기존 API를 사용한다.
- 로딩 aria-busy/표시를 추가하고 reduced-motion을 존중한다. 실패와 조회 전 상태를 빈 일정으로 오해하지 않게 유지한다.

## 검증
- 관련 통신/패키지 코드11개 PASS.
- 소스 화면: 상태 필터와 건수 연동, 추가 요청 없음, 오늘 이동 재사용, 상세 안전 표시, 공휴일, 다음 달 요청, 조회 실패/로그아웃 비움, 680/1040/1440 너비 PASS.
- 오른쪽 보조 모니터의 비포커스 Electron 시험 창을 사용했다. 가상 네트워크 자료이며 실제 운영 일정 대조는 아니다.
- 소스 화면 캡처 D:/GPT/tmp/moaon-month-design.png 직접 확인.

## 범위
- 일정 편집/서버 변경 없음. 외부 자동 업데이트 주소와 서명은 미설정 상태다.
- 로컬 관리형 배포와 패키지 최종 검증 결과는 아래에 기록한다.

## 최종 결과
- 패키지 화면 검증 PASS, 라이트/다크 캡처 직접 확인. 52파일/버전/의존성 검사 PASS. SHA256 778045965e369c0ca3a23abea152a4c494506aa1428fef965856216411a0e0cd.
- D:/GPT/Apps/Moaon/releases/0.75.0 배치와 복사 후 재검증, D:/OneDrive/바탕 화면/모아온.lnk 갱신 완료. 현재·이전·실행 중 관리형 버전 보존 정책 유지.
- 로그: D:/GPT/tmp/p487-tests.log, p487-ui.log, p487-package-ui.log, p487-package-verification.json, p487-local-publish.log.
