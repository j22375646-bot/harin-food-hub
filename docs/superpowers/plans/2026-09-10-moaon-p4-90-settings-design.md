# P4-90 — 앱 설정 탐색 / 0.78.0

## 변경
- 제목을 앱 설정으로 정리하고 화면/업데이트/조회 연결/사업장/플랫폼 API/출고 기록의6개 바로 이동을 추가했다.
- 항목 제목에 키보드 초점을 옮기고 스크롤한다. reduced-motion은 즉시 이동한다. 출고 기록은 접힌 영역만 열고 실제 조회 버튼은 사용자가 실행한다.
- 설정의14px 설명/44px 조작부/20px 제목/테두리를 정돈했다. 좁은 화면의 바로 이동은2열이다.
- 실제 연결·비밀번호·API 키 저장·업데이트 실행 경로는 변경하지 않았다. 프린터는 기존 별도 점검 진입을 유지한다.

## 검증
- 업데이트/패키지 코드13개 PASS.
- 소스 설정 UI:680/1040/1440/2200 너비, 카드 순서/가로 넘침/최대폭,6개 항목 초점,출고 기록 펼침과 닫힘 PASS.
- 가상 네트워크 응답의 격리 Electron을 오른쪽 보조 모니터에서 비포커스로 실행했다. 실제 로그인/프린터 출력/키 저장 검증은 아니다.
- 로그 D:/GPT/tmp/p490-tests.log,p490-ui.log. 최종 화면/패키지 결과는 아래에 기록한다.

- 라이트 캡처에서 제목/바로 이동과 카드의 시작선 차이를 발견해 공통1160px 폭과 중앙 정렬을 적용한 후 패키지를 재생성했다.
- 격리 시험은 개발용 Electron으로 실제 앱 코드를 실행하므로 프로그램 정보의 버전이 Electron44.2.0으로 표시될 수 있다. 배포0.78.0은 패키지 매니페스트/전체 파일 검증으로 별도 확인한다. 시험 자료 안내를 화면에도 추가했다.

## 최종 검증
- 패키지 UI PASS:6개 이동/초점,출고 기록 펼침/닫힘,라이트·다크,reduced-motion,설정 너비/배치. 최종 정렬/다크 캡처 직접 확인.
- 전체52파일/버전/의존성 PASS. SHA256 9ebbfb502d05bef0ab3144122c378fcbbd6d336fc5c7efd57c29bae5827637c7.
- 로그 D:/GPT/tmp/p490-ui-final.log,p490-package-ui.log,p490-package-verification.json. 캡처 moaon-settings-design-light.png/dark.png.

## 로컬 배포
- D:/GPT/Apps/Moaon/releases/0.78.0 배치·복사 후 재검증, D:/OneDrive/바탕 화면/모아온.lnk 갱신 완료. 현재/이전/실행 중 관리형 버전 보존. 사용자 앱 강제 종료 없음.
- 로그 D:/GPT/tmp/p490-local-publish.log. 외부 자동 업데이트는 주소/서명 미설정으로 비활성이다.
