# P4-81 — 고객·CS 조회 상태와 복구 / 0.69.0

## 변경
- 조회 전/조회 중/조회 실패/현재 범위 접수 없음/검색 결과 없음을 구분했다.
- 조회 실패와 빈 조회는 다시 조회, 검색 결과 없음은 조건 초기화 버튼을 제공한다. 초기화 후 검색창 DOM 초점을 복구하며 추가 조회하지 않는다.
- 조회 중에는 건수와 페이지에 조회 중을 표시한다. 실패 후에는 검색 범위를 알 수 없음을 명시한다.
- 일부 자료 응답에서는 빈 결과에도 전체 접수를 대표하지 않는다는 안내를 표시한다. 빈 결과를 처리 완료나 전체 미처리0건으로 해석하지 않는다.
- 기존 스타일의 18px 제목·14px 설명과 42px 이상 버튼을 사용해 안내 패널을 정렬했다.

## 검증
- 통신/패키지 코드9개 PASS: D:/GPT/tmp/p481-tests.log.
- 소스 UI53항목 PASS: D:/GPT/tmp/p481-ui.log. 기존47항목과 실패 재시도·조회 중·초기화 상태·빈 결과 복귀·부분 조회 경고·빈 조회 재시도6항목.
- 오른쪽 보조 모니터 right:true/focused:false. 가상 DB/네트워크 → 실제 로더/transport/IPC/DOM 시험이며 실제 로그인 자료 대조가 아니다.
- D:/GPT/tmp/moaon-cs-empty.png를 직접 확인했다.

## 운영
- 서버 변경/배포 요청 없음. 기존 서버와 호환된다. 본문/이력 등의 앞선 서버 확장은 Vercel 배포 한도로 운영 적용 대기다.
- 새 파일·프로필·로그·패키지는 D:/GPT 아래 저장한다.

## 최종 패키지
- 0.69.0 D 폴더 빌드 완료. 전체51파일/버전/의존성 대조 PASS.
- app.asar SHA256: bc931bc52d2f7d47c261c0203b3609316561368cf87d89cda5839ef4e7338856.
- 패키지 UI53항목 PASS: D:/GPT/tmp/p481-packaged-ui.log. 오른쪽 비포커스 확인. 개발 Electron 호스트에서 실제 app.asar를 로드한 격리 시험이며 설치 EXE/실자료 검증과 구분한다.
- 실행: [모아온 0.69.0 열기](../../../desktop/dist/p481/모아온%200.69.0%20열기.lnk). --display-right 인수. 기존 앱을 종료하고 실행한다. win-unpacked 폴더 전체를 유지한다.
