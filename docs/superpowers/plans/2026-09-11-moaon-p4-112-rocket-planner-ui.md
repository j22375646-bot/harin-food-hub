# P4-112 · 상품 목록 밀도와 로켓그로스 입고 판단 (0.98.0)

## 사용자가 할 수 있는 일

- 상품의 여러 채널 박스를 한 줄 목록으로 정리했다. 상품명·직접 재고·플랫폼 상태를 나란히 배치하고, 내부 박스·중복 패딩·카드 간 여백을 줄였다. 좁은 화면은 줄을 나눠 글자 크기를 유지한다.
- 로켓그로스에서 현재 재고, 최근 30일 판매량, 하루 평균 판매량, 예상 소진일/재고일수, 30일 뒤 잔량과 부족 수량을 확인한다.
- 목표 보유일 14/30/45/60일에 맞춰 보충 수량을 즉시 계산한다. 목표 대비 현재 재고 충족률을 짧은 막대로 표시한다.
- 전체/14일 내 소진/품절/자료 확인 필터, 소진 임박순/판매량순/상품명순 정렬과 검색을 지원한다. 기존 관리 대상의 품절 유지 규칙은 바꾸지 않는다.

## 웹허브 코드와 API 확인

- app/inventory/page.js → dashboard-route → app/unified-inventory-operations-center.js를 확인했다. RocketGrowthInventoryRow/ReplenishmentCard의 30일 평균 판매, 목표일, 보충량 계산을 참고했다.
- lib/coupang/mappers.js와 실제 DB raw_data 키를 조회했다. 현재 응답은 inventoryDetails.totalOrderableQuantity와 salesCountMap.SALES_COUNT_LAST_THIRTY_DAYS를 제공한다. 소진일과 보충량은 API 직접 제공값이 아니라 추정이다.
- 기존 매퍼가 누락된 판매량을 0으로 바꿨던 영향을 피하도록 raw API 판매 필드 존재 여부를 검증한다. 판매자료 누락/0개/6시간 지난 자료는 예측을 보류한다. 키·원본 응답은 앱에 전달하지 않는다.
- 30일 뒤 잔량은 마지막 수집 시점부터 같은 판매 속도·추가 입고 없음 가정이며 음수 대신 부족 수량을 따로 표시한다. 보충량만 올림하고, 단순 예측으로 쿠팡에 입고 신청이나 수량 변경은 하지 않는다.

## 적용·검증

- 코드 검증 370/370 PASS. 목표일별 재계산, 소수 판매 속도, 품절, 판매 이력 없음, 오래된 자료, API 필드 누락을 검증했다.
- 운영 배포 dpl_E5ySUyQ4G5UorYFv5dxThSrVV6sK READY. 인증된 stock GET200, rocketStatus READY. 실제 두 옵션의 수량/30일 판매는 120/168, 145/102이며 모두 최신 자료였다. 첫 옵션의 30일 목표 보충량은 48개다.
- 실제 Electron의 가상 자료 화면에서 목표일 변경, 품절 유지, 자료 노후화 시 예측 숨김, 680/1000/1440 크기 넘침, 상품 필터·상세 동작을 확인했다. 오른쪽 보조 모니터에 표시하며 포커스를 가져오지 않는다.
- 패키지 원본57개 일치, SHA256 4279f64cce3b17c054bff2de08555698d8d4644de437c8a47411522dfbb5c232.
- 전체 화면 검증 기록: D:\GPT\tmp\p4112-ui-suite. 코드 기록: D:\GPT\tmp\p4112-final-tests.log.
- 최종 패키지 전체17개 화면 PASS. D:\GPT\Apps\Moaon\releases\0.98.0 설치 및 바탕화면 모아온.lnk 갱신 완료. 설치 패키지 재고 검증도 PASS: D:\GPT\tmp\p4112-installed-stock.log. 실행 중인 0.97.1 사용자 창은 종료하지 않았으며, 다시 열면 0.98.0이 적용된다.

## 범위

DB 스키마 변경 없음. 상품 원본·채널 재고·실주문 변경 없음. 추정은 입고 준비를 돕는 값이며 계절성, 행사, 품절 기간의 수요 손실과 향후 입고를 반영하지 않는다. 화면의 계산 안내에 같은 제한을 표시한다. 기존 API 수집 주기·사업장 권한·재고 등록 방식을 유지한다.
