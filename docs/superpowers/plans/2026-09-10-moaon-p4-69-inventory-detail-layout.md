# P4-69 — 상품 상세 배치·목록 복귀 / 0.58.0

## 변경
- 상품 상세를 긴 목록의 끝에 덧붙이던 방식에서, 목록을 잠시 숨기고 같은 영역에 전체 너비 상세를 여는 방식으로 바꿨다.
- 목록으로 버튼 또는 Escape로 닫으면 페이지/검색/필터를 유지하고 원래 선택 상품으로 초점을 돌린다. 상세 진입 시 창 안의 스크롤만 상세 시작점으로 이동한다.
- 재고 자료와 연결 상품/가격 정보를 두 열로 정리했다. 700px 이하에서는 한 열로 배치한다. 가격과 항목 이름의 글자 크기를 구분하고 값은 항목별로 정렬했다.
- 선택한 필터에 맞는 채널, 다중 연결 검토가 필요한 채널을 먼저 표시한다. 채널 데이터/가격 판정/조회 방식은 바꾸지 않는다.
- 이번 수정은 앱 UI만 변경한다. 이전 서버 응답으로도 상세 배치·복귀가 작동한다. P4-66~68 서버 기능은 여전히 운영 반영 대기이며 이를 이번 UI 완료와 혼동하지 않는다.

## 검증
- 앱 연결/통신 회귀 111 PASS: D:/GPT/tmp/p469-tests.log.
- 소스 화면 25 PASS: 기존 20항목 + 상세가 목록을 대체하는 동작, Escape 후 기존 페이지/선택 초점 복귀, 검토 채널 우선 배치, 상품 상세 필드가 없는 구 서버 응답, 680px 화면에서 한 열 배치와 상세 가로 넘침 없음.
- 가상 DB → 실제 로더 → transport → Main IPC → DOM 시험이다. 오른쪽 보조 모니터, focused:false. 사용자 마우스/키보드 원격 조작 없음.
- D:/GPT/tmp/p469-ui.log, moaon-inventory-worklist.png, moaon-multiple-connections.png.
- 패키지 화면도 25 PASS: D:/GPT/tmp/p469-packaged-ui.log. 개발 Electron 호스트가 완성된 app.asar를 격리 프로필에서 로드한 시험이며 설치 EXE/실제 로그인 세션 인수와 구분한다.
- 폴더 빌드 exit 0, manifest 0.58.0, 앱 파일 5개 소스 바이트 일치. app.asar SHA256: 2592a2c9efaec241ee73da56c4cc5f50f7adbc410d10cc8381b13c2c11a7f55f.

## 전달과 남은 작업
- D:/GPT/moaon/desktop/dist/p469/win-unpacked/MoaonPreview.exe.
- 이번 화면 배치 수정에 서버 배포는 필요하지 않아 Vercel 배포를 요청하지 않았다. P4-66에서 확인한 일일 한도의 해제 여부는 재확인하지 않았다.
- 새 상품·가격·다중 연결 데이터는 P4-66~68 운영 배포 후 실제 로그인 자료 대조가 필요하다.
- 고객·CS 본문/답변 이력 조회가 다음 기능 작업이다.

- 바로가기 D:/GPT/moaon/desktop/dist/p469/모아온 0.58.0 열기.lnk (--display-right). 기존 앱을 닫고 실행한다. win-unpacked 폴더 전체를 유지한다. 설치 앱은 교체하지 않았다.
- 소스·로그·스테이징·검증 프로필·빌드는 D:/GPT 아래에 저장했다.
