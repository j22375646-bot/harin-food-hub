# P4-97 — 분석 조회 상태와 재시도 UI

## 변경
- 0.84.0. 분석의 로딩/조회 실패/연결 전 상태를 목록 안의 안내 카드로 구분한다. 실패를 저장 보고서 없음이나 조회 전으로 표시하지 않는다.
- 실패 안내의 다시 조회 버튼을 실제 readInsights에 연결했다. 조회 중 버튼을 잠그고 중복 요청을 막으며 성공하면 목록으로 복구한다. 기존 검색 조건과 5분 재조회 기준을 유지한다.
- 미확인 금액은 판단 보류로 유지한다. 동작 줄이기 설정에서는 로딩 카드와 분석 지표 애니메이션을 멈춘다.

## 검증
- 전체 코드 329/329 PASS: D:/GPT/tmp/p497-tests.log.
- 배포 코드의 10개 UI 흐름 모두 PASS: D:/GPT/tmp/p497-ui-suite/summary.json. 현재 소스와 패키지 52파일 대조 PASS.
- 분석 시험에 실패 카드/재시도/지연 중 상태/버튼 잠금/중복 요청/복구/동작 줄이기 검증을 추가했다. 가상 자료 안내 배너 추가 후 최종 재검증 PASS: D:/GPT/tmp/p497-insights-final.log.
- 오른쪽 보조 모니터의 격리 Electron에서 포커스 없이 실행했다. 실패 화면 캡처를 직접 확인했다: D:/GPT/tmp/moaon-insights-error.png.
- 실제 로그인 자료나 운영 API 응답 속도 검증은 아니다. 이번 작업으로 네트워크 로딩 시간이 줄었다고 주장하지 않는다.

## 로컬 반영
- D:/GPT/Apps/Moaon/releases/0.84.0 배포와 기존 D:/OneDrive/바탕 화면/모아온.lnk 갱신 완료.
- 관리 정책은 현재 + 이전 + 실행 중 버전 보존이다. 실행 중인 사용자 앱을 종료하지 않았다. 현재 앱을 닫고 같은 바로가기를 다시 열면 최신본이 실행된다.
- 배포 로그: D:/GPT/tmp/p497-local-publish.log. 외부 업데이트 주소/서명과 미반영 서버 기능은 이번 범위에 포함되지 않는다.
