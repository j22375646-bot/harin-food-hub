# P4-91 — 재고/CS 재방문 렌더링 / 0.79.0

## 측정과 변경
- 두 페이지 ensure가 숨김에서 돌아올 때마다 render를 호출해 표시 목록을 교체함을 확인했다. 현재 코드에서는 새 조회/응답/필터/선택/clear가 각각 render를 호출하므로 재방문에서 중복 호출을 제거했다.
- 새 조회와 오류 재시도 조건은 기존대로 유지한다. 데이터 저장/권한/서버 요청 캐시 정책 변경 없음.
- 가상 자료(재고52상품/CS53접수)로 설정↔대상 페이지20회 왕복. performance.now로 showRoute의 동기 처리 시간을 측정했다. 화면 표시 완료/실제 API 응답 지연 측정은 아니다.

| 화면 | 변경 전 중앙값 / p95 | 변경 후 중앙값 / p95 | 목록 교체 |
|---|---|---|---|
| 재고·상품 | 12 / 18ms | 11.3 / 16.6ms | 20 → 0회 |
| 고객·CS | 7.3 / 10.5ms | 4.6 / 6.3ms | 20 → 0회 |

- 한 PC의 한 시험 실행이며 성능 수치는 변동 가능하다. 확정 회귀 기준은 같은 DOM 유지와 교체0회다. 시간 임계값으로 통과 여부를 정하지 않는다.
- 시험 로그 D:/GPT/tmp/p491-inventory-before.log,p491-cs-before.log,p491-inventory-after.log,p491-cs-after.log.

## 검증
- 두 소스 UI의 기존 검색/필터/페이지 경계/상세 이동/추가 요청 없음/실패/로그아웃/구서버 응답 회귀 PASS.
- 테스트에서 MOAON_EXPECT_REUSE=1이면 목록 DOM과 상세 제목 DOM의 재방문 유지도 검증한다.
- 오른쪽 비포커스 Electron, 가상 DB/네트워크 시험이다. 실자료 조회 속도 측정은 아니다.

## 최종 검증·배포
- 통신/패키지 코드18개 PASS. 패키지 재고·CS UI 회귀 및 목록/상세 DOM 재사용 PASS. 오른쪽 비포커스/새로 조회/필터/늦은 응답 비움 유지.
- 52파일/버전/의존성 PASS. SHA256 f7d4c4c4dfbb04a23a48198a9033b9bf5499638525991374fa9c4fb784529a3a.
- 로그 D:/GPT/tmp/p491-tests.log,p491-inventory-package.log,p491-cs-package.log,p491-package-verification.json. 패키지 재고 캡처 직접 확인.
- D:/GPT/Apps/Moaon/releases/0.79.0 배치·복사 후 검사, D:/OneDrive/바탕 화면/모아온.lnk 갱신 완료. 사용자 앱 강제 종료 없이 현재/이전/실행 중 관리형 버전 보존. 로그 p491-local-publish.log.
- 외부 업데이트 주소/서명과 실제 서버 데이터 응답 측정은 남아 있다.
