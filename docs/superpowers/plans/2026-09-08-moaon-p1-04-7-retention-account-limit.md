# P1-04-7 Retention and account login quota Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Follow each task's TDD and commit gate.

**Goal:** Bound stale auth-counter retention and share login quota across username/email for the same trusted profile.
**Architecture:** Candidate SQL prunes only stale counters and preserves consumption row locks. An opt-in post-profile login limiter reuses the existing subject RPC with a UUID-derived domain-separated subject.
**Tech Stack:** Existing Node24, PostgreSQL17, PGlite, Supabase RPC; no dependency changes.
**Spec:** `docs/superpowers/specs/2026-09-08-moaon-auth-retention-account-limit.md`

## Global Constraints

Preserve production login/UI/password/business APIs and environment configuration. No production SQL, Auth activation, real emails, paid resources or production-data copies. Candidate SQL stays outside migrations. No EXE release. Commit task files only; no scratch reports. No subagents from workers. Parent owns native DB startup, final build/version/release and reports.

## Task 1: Safe bounded counter retention

Files: modify `lib/tenancy/sql/auth-request-limit.sql` and `auth-request-admission.sql`; create `lib/tenancy/sql/auth-request-retention.sql`, `lib/tenancy/auth-request-retention.js`, `test/tenant-auth-request-retention.test.js`, `test/integration/tenant-auth-request-retention.native-test.js`. Existing adjacent tests may be updated only if needed to assert real changed behavior.

Binding constraints: preserve production login/UI/APIs; no real cloud/SQL activation or emails; no new dependencies; SQL candidates only. Read the linked spec and existing bounded-auth-rpc/SQL consumers. No child agents.

- [ ] RED: test real SQL retention with synthetic rows, keeping current/GLOBAL/future rows and deleting old rows. Each table 501 old rows should yield 500 then1. Use exactly older-than DB clock minus24h, not supplied application time. Verify service role permitted, anon/authenticated/public denied, rollback, repeat application, invalid response shape/transport/timeout of adapter. Record actual failing behavior before implementation.
- [ ] Implement fixed zero-argument `public.moaon_prune_auth_request_limits()` returning JSON `{requestDeleted,ipDeleted}` integers 0..500. Private schemas, SECURITY INVOKER empty search_path, RLS retained, revoke execute from public/anon/authenticated, grant only service_role and DELETE on these two counter tables. Never delete GLOBAL or any other table. New candidate requires the updated consumer candidates to be installed together before activation.
- [ ] Use a captured DB cutoff `clock_timestamp()-interval '24 hours'`; stale candidate selection ordered by started_at and PK, LIMIT500 FOR UPDATE SKIP LOCKED, delete selected PKs only, sequentially subject table then IP table. Add repeat-safe indexes `(started_at,kind,subject_hash)` and partial `(started_at,key_hash) where scope='IP'`. No caller knobs, no loops until empty, no scheduled job.
- [ ] Close the insert-do-nothing/select gap in both consumers with lock-preserving UPSERT RETURNING. Example subject acquisition: `insert into moaon_auth.request_limits as limits(kind,subject_hash) values(p_kind,p_subject_hash) on conflict(kind,subject_hash) do update set used=limits.used returning * into v_bucket;` Sample DB time only after this acquisition. Apply corresponding GLOBAL then IP upserts in admission. Preserve every quota and reset condition; do not duplicate whole functions in retention SQL.
- [ ] Add `createAuthRequestRetention({rpcClient,timeoutMs=10000})` returning frozen `{prune}`. `prune()` invokes only zero-argument prune RPC via existing createBoundedAuthRpc. Reject any supplied arguments (not silent knobs); validate exactly own keys requestDeleted/ipDeleted, bounded integer counts, return frozen copy. Errors sanitized `AuthRequestRetentionError`, code AUTH_REQUEST_RETENTION_UNAVAILABLE/status503; no raw RPC error, no retries or late follow-up. Invalid config generic TypeError. Server-only guard inherited transport.
- [ ] Native test follows existing disposable loopback harness guard (127.0.0.1:55437/user moaon_test_admin/supervisor postgres; pool<=4, own random database and only own roles cleanup). Prove separate backends: locked stale counter is skipped; consumer holds locks before clock evaluation; cleanup owns stale row then consumer waits, after cleanup commit consumer recreates safely; rollback restores deleted records; concurrent pruners never duplicate rows or remove fresh counts. Cover both subject and IP consumption. No external DB and no credentials in report. Parent runs native when file ready.
- [ ] GREEN focused new tests plus existing request-limit/admission tests. Run full pnpm test once before commit, not after each edit. Self-review, commit, write task-1-report with RED/GREEN/commands/output and concerns. Parent verifies native and final build.

## Task 2: Trusted profile ID login quota

Files: create `lib/tenancy/account-login-limit.js` and `test/tenant-account-login-limit.test.js`; modify only the narrow optional accountLimit path in `lib/dashboard-auth.js`. No schema, routes, UI or production dependency wiring.

Binding constraints: preserve all existing behavior when accountLimit omitted, existing admission and subject quota stay first, no cloud/SQL activation, no real provider or emails, no dependencies, no child agents. Task1 retains existing quota RPC names/policies.

- [ ] RED: actual authenticateAccount integration alternating a synthetic profile's username/email ten successful attempts then eleventh gets LOGIN_RATE_LIMITED before provider/session writes; use real account adapter and PGlite quota SQL. Input userId/accountLimit metadata must not spoof trusted profile. Different profile IDs must not share budget. Rename username fixture but preserve ID to prove budget persists. Do not invent provider-specific email alias equivalence.
- [ ] Add `createAccountLoginLimit({rpcClient,hmacKey,timeoutMs=10000})` returning async `accountLimit({userId})`. Strict UUID v1..5/variant pattern like existing code, normalize hex lowercase, call existing createAuthRequestLimit with `{kind:'LOGIN',subject:'ACCOUNT:'+normalizedUuid}`. Thus HMAC preimage remains `JSON.stringify(['LOGIN','ACCOUNT:'+uuid])`; canonical ID is never raw SQL argument. Return existing frozen allowed contract. Reject invalid UUID before any RPC. The ACCOUNT colon namespace cannot collide with allowed login input syntax. Reuse key/transport validation rather than duplicating it.
- [ ] Extend authenticateAccount options with `accountLimit` (undefined=no change). If provided require function and existing `requestLimit` function at configuration time before DB/provider. Pre-input limit remains before profile lookup. After successful DB lookup, for active===true profile verify user_id UUID, email valid normalized and selected lookup field (username/email trimmed lower) equals normalized input; malformed/mismatched active profile returns sanitized LOGIN_AUTH_UNAVAILABLE. Truthy non-boolean active is malformed when accountLimit enabled; absent/inactive profile preserves old dummy-auth path without canonical quota. Do not modify lookup/registration/schema normalization behavior globally.
- [ ] Invoke bounded accountLimit({userId:profile.user_id}) only after that verification and before authClient creation, sessionFence.beginLogin, provider sign-in or session issuance. Only exact `{allowed:true}` proceeds; false gives existing429 LOGIN_RATE_LIMITED, errors/malformed/timeout503 LOGIN_AUTH_UNAVAILABLE. No continuation after timeout. Use existing boundedFencedDependency/exactLimitResult helpers; avoid copying limit transport. Caller request body never selects dependency or UUID.
- [ ] Tests include uppercase UUID equivalent identity, fixed independently computed HMAC fixture/no raw ID, invalid input, prior limiter denial prevents lookup, canonical false/malformed/rejection/late timeout prevent auth/fence/session, missing prior limiter rejected, omitted accountLimit legacy path, inactive/unknown profile no canonical attempt, wrong active profile denied, separate users and renamed aliases. Stub external provider only; use complete existing dashboard schema fixtures or narrowly scoped query fake preserving consumed side effects. No shallow source-text assertions.
- [ ] GREEN focused new login tests and old dashboard-auth/fenced-login/admission/recovery tests, full pnpm test once. Self-review, commit code/tests, write task-2-report with actual RED/GREEN evidence. Parent owns final whole-branch review/build/release.

## Release and next gates

Proposed1.49.0 only after both tasks reviewed. Actual login activation, trusted proxy resolution, policy tuning, retention scheduler, operator recovery review/resolution and real Auth/SMTP/HTTP gates remain. Account quota is not duplicate-account merge or email-change lifecycle support. Task1 and2 independently testable; parent runs full merged source tests, existing Vercel deployment verification and detailed Korean report.
