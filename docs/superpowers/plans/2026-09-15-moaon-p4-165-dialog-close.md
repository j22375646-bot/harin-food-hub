# P4-165 모든 대화창 상단 고정 닫기

데스크톱 0.147.3. 이벤트 기획, 일정, 업무/프로필, 재고/입고/상품 선택, 재고 확보, 업무 검색, 실행 확인, 업데이트 등 대화창에 공통 오른쪽 상단 × 버튼을 추가한다. 동적으로 생성되거나 내용이 교체되는 창도 적용된다. 기존 하단 취소·닫기를 유지하고 상단 버튼은 그 핸들러를 호출한다. 비활성 버튼과 cancel 취소 방지를 우회하지 않는다. 모달 스크롤 안에서 sticky 위치를 사용한다.

검증: 전체 데스크톱 시험451/451 통과. 격리된 실제 Electron 소스/설치본 각각 PASS: 기본 대화창12개 적용, 700/1440px 라이트·다크 스크롤 위치/클릭 가능 여부, 재고 취소, 비활성 잠금, 동적 생성/교체, 취소 이벤트 차단. 오른쪽 보조 모니터에 검증 창 표시, 사용자 입력/운영 쓰기 없음. 설치 ASAR114개 소스 파일 일치. diff check 통과.

산출물 distribution-20260914-235914-509. Ed25519 서명으로 moaon-stable 배포 완료. 설치 D:/GPT/Apps/Moaon/releases/0.147.3, 바탕화면·시작메뉴·작업표시줄 바로가기 및 current.json 갱신. 웹 버전1.64.0 변경 없음.

공개 업데이트 검증: 이전 버전0.147.2 가상 환경에서 AVAILABLE→설치 파일 다운로드→Ed25519 검증 READY 통과. 설치파일 자동 실행은 하지 않았으며 별도 설치 ASAR로 UI 검증했다. 최신 버전 CURRENT 확인은 최초와 재시도 모두 ERROR로 끝나 중복 업데이트 미표시 검증은 미완료이다. 서명 검증/업데이트 제한을 완화하지 않았다.
