# P4-180 업무비서 조회 API

## 범위
- 서버 1.64.5 / 데스크톱 0.147.17.
- GET /api/moaon/businesses/[tenantId]/assistant. 기존 소유자 세션 및 사업장 검증을 조회 전후 적용한다. 조회 중 멤버십/세션 변경 시 결과를 폐기한다.
- 기존 레거시 자료의 하린 사업장 경계를 유지한다. Hermes용 외부 토큰 인증은 아직 제공하지 않는다.
- 저장 주문의 채널별 판매자배송 상태 건수, 본인 배정 미완료 마감 업무, 채널별 미답변 문의, 네이버 주간 보고서 목록/제한된 근거를 반환한다.
- 원본 주문·고객 연락처/주소·인증키·업무 본문은 반환하지 않는다. DB·채널 수집·발급·출고·Telegram 발송을 실행하지 않는다.
- 부분 실패와 범위 초과는 null/UNAVAILABLE/PARTIAL로 표시한다. retrievedAt은 조회 시각이며 sourceAsOf=null은 수집 시각 확인 필요를 의미한다.
- 데스크톱은 고정 URL GET, 본체 프로세스의 임시 허용, IPC 송신자 검증, 응답 크기/형식 검증을 거쳐 4개 자료 카드에 표시한다.

## 검증
- 관련 서버 55개 통과. 전체 서버 회귀 3017개 중 기존 변경 이력 누락 1개 발견, 새 버전 기록 후 해당 항목 및 신규 API 7개 재검증 통과. 다른 3016개는 통과.
- 데스크톱 기존 452개 및 새 통신/정책 3개 통과.
- source/packaged Electron: 700/1060/1660 너비 × 밝음/어두움 × 3개 탭, 18조합 각각 통과. 부분 실패 표시, 네 자료 카드, 초안 복원, 비활성 외부 연결, 헤더 정렬 검증.
- 기본 Turbopack은 이 PC의 외부 node_modules 링크를 거부했다. 저장소 Windows webpack 설정으로 로컬 빌드 통과. Vercel Linux Turbopack 운영 빌드 통과.
- 패키지 121개 파일 일치. distribution-20260916-020400-164.
- 운영 배포 dpl_AtEsEGLZ4tSKJ8KaCcXkoyBsmeGS READY, harin-cafe24-sync.vercel.app 별칭 반영. 비로그인 API 401 및 X-Harin-Version 1.64.5 확인.
- 실제 설치 0.147.17을 오른쪽 모니터에 표시하고 소유자 로그인 상태 조회 성공. 네이버/카페24/쿠팡 발급 전 각 1건, 오늘 마감 업무 2건, 미답변 문의 각 0건, 네이버 보고서 표시. 검증 시점 저장 자료이며 실시간 채널 수집 결과가 아니다.
- 화면 D:/GPT/tmp/p4180-installed-data.png. Hermes/Telegram 실제 연결은 미완료이며 입력란을 비활성 상태로 유지한다.

## 후속
Hermes 외부 조회 전용 자격 증명 발급/폐기와 허용 범위, Telegram 봇 보안 저장·본인 계정 확인·단일 수신 경로, 실제 왕복 대화 확인을 순서대로 구현한다. 현재 사용자 세션 쿠키/DB 키를 Hermes에 전달하지 않는다.
