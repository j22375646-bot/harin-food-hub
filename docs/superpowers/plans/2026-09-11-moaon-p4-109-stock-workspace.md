# P4-109 · 재고 운영 화면 정리 (0.95.0)

## 재고 UI와 기능

- 기한 임박(30일 이내), 기한 경과, 수량0 빠른 필터와 제조번호 기준 건수 표시.
- 검색, 유통기한 빠른 순/상품명/최근 수정 정렬, 조건 초기화. 단위가 다른 재고는 합산하지 않고 현재 목록의 단위별 수량을 보여준다.
- 상품명/제조번호, 큰 수량, 제조일/유통기한, 보관방법, 상태 뱃지를 정렬하고 좁은 화면에서는 재배치한다.
- 편집창에서 정보 수정, 입고, 출고를 구분한다. 변경 전후 수량 미리보기, 초과 출고 차단, 변경 사유 필수, 기존 메모에 조정 기록 추가. 기존 서버 revision 충돌 검사와 변경 이력을 그대로 사용한다. 별도 회계용 입출고 원장을 신설한 것은 아니다.
- 상품/보관방법/단위를 복사하여 새 제조번호를 등록한다. 수량/제조번호/날짜/메모는 새로 입력한다.
- 조회 중 스피너, 오류 시 0건 대신 확인 필요, 필터별 빈 상태/재시도, 이전 사업장 응답 폐기, 키보드 포커스/동작 줄이기/어두운 테마 지원.

## Vercel 오류 조사와 운영 서버

- 이전 배포에서 Not authorized를 받았으나 이번 whoami와 동일 프로젝트 inspect는 정상. 앞선 재인증 필요 단정은 근거가 부족했음을 정정한다.
- 동일 계정의 배포 재시도 성공. 원래 거절의 세부 원인은 제공된 응답만으로 확정할 수 없다. 지속적인 로그인 만료나 프로젝트 접근 불가로 확인된 것은 아니다.
- 운영 배포 dpl_5bSsZskM6UwgkERoSYfsBM5E53ta, READY, https://harin-cafe24-sync.vercel.app 별칭 연결 완료. 서버는 기존 e6448c7 재고 API/상품 조회 수정이며 이번 변경은 데스크톱 UI다.
- 로그인된 웹허브에서 동일 사업장 stock GET 200/0건, inventory GET 200/41개 확인. 이전 P4-108 서버 배포 차단과 상품503은 해소됐다.
- 운영 재고를 테스트 목적으로 추가/차감하거나 실주문 출고하지 않았다. 실제 두 PC 동시 입력은 별도 검증이며, 수정 충돌은 가상 서버 앱 시험과 이전 DB 트랜잭션 시험으로 검증했다.

## 검증

- 앱 코드351/351 PASS. 공유 DB/서버 코드 변경 없음.
- 격리 Electron의 가상 서버에서 등록, 수정 충돌, 출고 사유, 보유 수량 초과 차단, 새 제조번호, 단위별 합계, 기한 정렬/필터, 오류 후 복구, 늦은 응답 폐기, 동작 줄이기, 어두운 편집창과 화면 너비 확인.
- 오른쪽 보조 모니터에서 포커스를 가져오지 않고 검증했다.
- 최종 패키지/설치 결과는 아래에 기록한다.

## 최종 인도

- 최종 재고 화면 시험 PASS: D:\GPT\tmp\p4109-stock-verified.log. 설치된 같은 패키지의 재고 화면 재시험도 PASS: D:\GPT\tmp\p4109-installed-stock.log.
- 9개 메뉴 × 2개 너비 × 밝은/어두운 테마 공통 UI PASS: D:\GPT\tmp\p4109-design-verified.log. 잘못된 재고 응답에서 발생한 초기 화면 오류를 보완했고 실패 로그는 보존했다.
- 상품/주문 화면 회귀는 직전 0.95.0 후보에서 PASS. 그 후 재고 전용 어두운 버튼 대비/응답 검증만 수정했고 최종 재고/전체 메뉴 검증을 다시 했다.
- 설치 파일56개 원본 일치. SHA-256 45bc5fa6f59442aca4726a271922f708218784e734ccca50e175580acbc96a35.
- D:\GPT\Apps\Moaon\releases\0.95.0 및 D:\OneDrive\바탕 화면\모아온.lnk 갱신. 실행 중 0.94.0은 종료하지 않았다. 같은 바로가기 재실행으로 새 버전을 사용한다.
