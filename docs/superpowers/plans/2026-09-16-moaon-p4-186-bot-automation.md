# P4-186 봇별 자동화 (2026-09-16)

## 적용
- WORK/SOLO 별 예약, 요일, 시간(KST), 브리핑 자료(주문/문의/업무), 변화 감지 시간대를 독립 저장한다.
- 수신처는 각 봇 연결 설정이 유일한 기준이다. 구버전 공통 발송은 마이그레이션 시 끄고 기존 예약은 WORK로 이전한다.
- 매일 09:00 WORK 예약을 유지하며 SOLO 정기 발송은 기본 꺼짐, 업무만 선택한다.
- 상태/이력, 다음 예약, 봇별 시험 발송, 브리핑 웹 허브 바로가기 제공.
- 조회 키 발급자의 업무라는 범위를 표시한다. 개인 Telegram 사용자와 동일인으로 추정하지 않는다.
- 작업기는 봇별 예외를 격리하고 상태를 보고한다. 서버 무응답은 UI의 5분 지난 접속 경고로 표시한다. 독립 외부 장애 알림 감시는 아직 없다.
- Telegram 업무등록/다시알림 콜백은 다음 단계다. 현재 바로가기는 로그인된 웹 허브로 이동한다.

## 검증
- 서버 회귀 3028 통과, 데스크톱 회귀 457 + 신규 2 통과.
- Python 작업기 5 통과. SQL 봇 격리, revision/disabled 검증, 중복 claim, UNKNOWN 보존, 권한 거부 통과.
- 소스/패키지 Electron: 세 너비 x 두 테마, 개별 저장/이력 6 상태 통과.
- 0.148.1 패키지 선언 파일 130개 일치, 서버 1.66.0 READY.
- 설치 앱 실제 본인 세션에서 WORK/SOLO 시험 요청; 두 건 모두 2026-09-16 06:00 KST SENT, worker_result OK.
- 실제 오전 9시 예약 발송 시각은 아직 도래 전이다. 스케줄 판정 테스트와 다음 예약 표시를 검증했다.
- 인증/기존 봇 대화/계정 설정은 변경하지 않았다.

## 첫 진입 보완
- 공통 READ와 AUTO_READ가 동시에 실행되어 main 프로세스의 busy guard에 걸리는 현상을 공통 조회 Promise 순서로 해결했다.
- 지연/중복 요청 차단을 재현한 Electron 시험에서 첫 진입, 개별 설정 저장, 봇별 시험 이력을 검증했다.
