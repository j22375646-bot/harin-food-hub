# P4-67 — 연결 상품명·가격 상세 / 0.56.0

## 상태
구현·관련 테스트·D 폴더 빌드·가시 패키지 검증 완료. P4-66에서 확인한 Vercel 일일 무료 배포 제한으로 서버는 미적용 상태이며 이번에는 배포 요청을 반복하지 않았다. 운영 서버는 P4-65 기준이다.

## 변경
- 재고 상세에 연결된 상품명, 저장 가격 또는 쿠팡 옵션 최저/최고 가격, 가격 자료 시각을 표시한다. 연결 상품명으로 목록 검색도 가능하다.
- Cafe24는 실제 상품 레코드의 이름/가격을 사용한다. 이름이 없을 때 연결명만 보충하며 가격은 연결 테이블의 다른 숫자로 대체하지 않는다.
- 네이버는 커머스 상품 연결의 이름/가격만 가격 자료로 인정한다. 광고 연결 이름은 참고로 보여주지만 저장된 금액을 상품 가격으로 표시하지 않는다. 재고와 가격 모두 기존 웹허브의 동일 커머스 판정을 사용한다.
- 쿠팡은 연결 옵션 모두에 유효한 양수 가격이 있을 때만 최저~최고 범위를 제공한다. 하나라도 누락/0/음수이면 가격 확인 필요다. 가장 오래된 옵션 자료 시각으로 신선도를 표시한다.
- 쿠팡 가격은 연결 상품 옵션 공통 자료이며 판매자배송/로켓그로스 전용 가격을 확인한 것이 아니라는 설명을 화면에 붙였다.
- 6시간이 지난 가격 자료는 갱신 필요로 표시한다. 가격 비교율·마진·가격 수정 기능은 추가하지 않았다.
- 앱은 이름 200자, 숫자 범위/순서, 채널별 자료 출처, 날짜 형식을 검증하고 허용 필드만 투영한다. 응답 크기 제한은 8MiB다. 구 서버의 상품 상세 필드 없는 응답은 확인 필요로 표시한다.

## 검증
- 관련 147 PASS: D:/GPT/tmp/p467-tests.log. 전체 서버/앱 테스트 재실행은 아니다.
- 네이버 판정 통일 후 로더 관련 14개 재PASS: D:/GPT/tmp/p467-final-loader.log. 소문자 source_type의 재고/가격 판정 일치도 확인했다.
- Cafe24 원본 가격·잘못된 가격의 확인 필요 처리, 네이버 광고 가격 제외, 쿠팡 전체 옵션 범위/누락/오래된 시각, 채널 자료 혼입 거절, 구 응답 호환을 검증했다.
- 소스와 패키지 창 각각 17 PASS: 기존 목록/필터/페이지/실패 회귀 + 원본 상품명 검색·가격 범위·광고 가격 제외.
- 가상 DB → 실제 서버 로더 → transport → Main IPC → DOM 시험이다. 오른쪽 모니터, focused:false 확인. 실제 운영 가격을 대조한 결과가 아니다.
- 패키지 UI는 개발 Electron 호스트가 완성된 app.asar를 격리 프로필에서 로드한 시험이다. 설치 EXE 또는 로그인 사용자 세션 인수와 구분한다.
- D:/GPT/tmp/p467-ui.log, p467-packaged-ui.log, moaon-inventory-worklist.png.
- 폴더 빌드 exit 0. app.asar manifest 0.56.0과 앱 파일 5개 소스 바이트 일치.
- app.asar SHA256: 4fa501b1658239abd7924f3d3698de25cd14ae0a52028ef430e55c417f57402c.

## 전달과 운영 대기
- D:/GPT/moaon/desktop/dist/p467/모아온 0.56.0 열기.lnk (--display-right)
- D:/GPT/moaon/desktop/dist/p467/win-unpacked/MoaonPreview.exe
- 기존 앱을 닫고 새 바로가기를 연다. win-unpacked 폴더 전체를 유지한다. 기존 설치 앱을 교체하지 않았다.
- 새 개발 파일·로그·스테이징·검증 프로필·빌드는 D:/GPT 아래에 저장했다.
- P4-66 배포 로그의 api-deployments-free-per-day / 24시간 후 재시도 안내가 마지막으로 확인한 제한이다. 이번 단계에서 한도 해제 여부를 재확인하거나 새 서버 빌드/배포를 실행하지 않았다.
- 한도 해제 후 P4-66/67을 함께 운영 배포하고 실제 로그인 상품/가격을 대조해야 한다. 자동 재시도 예약은 없다.
- 후속은 고객·CS 본문/답변 이력 조회와 상품 다중 연결 처리다.
