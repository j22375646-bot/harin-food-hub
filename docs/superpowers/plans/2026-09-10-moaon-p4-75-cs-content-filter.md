# P4-75 — 고객·CS 내용 상태 필터·조건 초기화 / 0.63.0

## 변경
- 내용 조회됨/일부 내용/저장 내용 확인 필요/복원 실패/본문 연결 대기를 개별 필터로 추가했다. 채널·유형·검색 조건과 함께 적용한다.
- 조건 초기화는 모든 필터·검색·정렬을 기본값으로 복구하고 첫 페이지 및 검색 입력 초점으로 돌아간다. 추가 서버 요청은 발생하지 않는다.
- 기본 조건이거나 조회 중일 때 초기화 버튼을 비활성화한다. 로그아웃/초기화 시 내용 상태도 전체로 복구한다.
- 필터 4개를 같은 너비로 정렬하고 아래 줄에 검색창과 초기화 버튼을 배치한다. 700px 이하에서는 두 열 필터와 전체 너비 검색/초기화로 재배치한다.
- 구 서버 응답은 본문 연결 대기로만 분류하며 내용 있음으로 오인하지 않는다. 서버 변경 없음.

## 검증
- 관련 코드 시험 119 PASS: D:/GPT/tmp/p475-tests.log.
- 소스 화면 43 PASS: D:/GPT/tmp/p475-ui.log. 5종 내용 상태 필터, 복합 조건, 초기화 정렬/페이지/초점/추가 요청 없음, 기본 버튼 비활성, 구 서버 필터 검증 포함.
- 가상 DB → 실제 로더 → transport → Main IPC → DOM. 오른쪽 보조 모니터 right:true/focused:false. 실제 운영 자료 인수와 구분한다.
- 목록 화면 D:/GPT/tmp/moaon-cs-availability.png를 직접 확인했다. 필터와 검색/초기화 정렬 정상.

## 운영 상태
- 서버 확장은 기존 Vercel 일일 한도로 운영 적용 대기다. 이번 앱 개선에서는 재배포를 요청하지 않았다.
- 현재 운영 서버에서도 기존 번호 검색·목록·상세 이동과 연결 대기 필터는 작동한다. 제목/본문/이력 데이터는 P4-71 서버 적용 후 제공된다.
- 모든 신규 파일·로그·검증 프로필·패키지를 D:/GPT 아래에 저장한다. 기존 설치 앱은 교체하지 않는다.

## 최종 패키지
- 빌드 exit 0: D:/GPT/tmp/p475-package.log. verify-package.cjs로 버전 0.63.0·앱 전체 51파일·의존성 PASS: D:/GPT/tmp/p475-package-verification.json.
- SHA256: 6ccee843030d1f136cd0150622c3594bee38e0a0cadacf918c98846fa15243a7.
- 패키지 화면 43 PASS: D:/GPT/tmp/p475-packaged-ui.log. 개발 Electron 호스트가 완성된 app.asar를 격리 프로필에서 로드했다. right:true/focused:false.
- 실행 바로가기 D:/GPT/moaon/desktop/dist/p475/모아온 0.63.0 열기.lnk (--display-right). 기존 앱을 닫고 실행하며 win-unpacked 폴더 전체를 유지한다.
