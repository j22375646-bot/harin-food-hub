# 모아온 P1-04-6 개발 보고서

> 완료 범위: P1-04-6 / 1.48.0 IP별·서비스 전체 인증 요청 제한 기반의 구현·검증·코드 배포. 최종 빌드·전체 2,217개 검사·독립 검토를 통과했다. 새 SQL과 인증 조합은 기존 운영 로그인에 활성화하지 않았다.

## 이번에 만드는 것

여러 사업장이 사용하는 모아온에서는 누군가 아이디를 계속 바꾸며 로그인을 시도할 수도 있다. 지난 단계의 계정별 제한만으로는 이런 요청 전체를 제한하지 못한다. 이번에는 계정별 확인 앞에 같은 IP와 서비스 전체 요청량을 확인하는 공통 제한을 추가한다.

요청 순서는 서비스 전체 → IP → 기존 계정별 제한 → 인증 처리다. 앞 단계에서 거절되거나 저장소가 응답하지 않으면 인증 처리로 넘어가지 않는다. 앞 단계에서 쓴 횟수는 뒤 단계의 거절로 돌려주지 않는 보수적인 방식이며, 세 단계를 한 번에 처리하는 단일 거래는 아니다.

| 범위 | 후보 기준 | 의미 |
| --- | --- | --- |
| 같은 IP | 5분 30회 | 여러 아이디를 바꾸어도 IP가 같으면 함께 계산 |
| 서비스 전체 | 5분 500회 | 여러 IP에서 들어오는 요청도 합산 |
| 계정별 | 이전 단계 정책 유지 | 로그인·복구·이메일 확인별 추가 제한 |

이 숫자는 실제 이용량에 맞춘 최종 운영 설정이 아니다. 사무실의 여러 PC는 하나의 IP를 공유할 수 있으며, 전체 제한은 공격 중 정상 사용자까지 차단할 수 있다. 실제 전환 전 정책 조정과 관측이 필요하다.

IP 원문 대신 서버 비밀키로 계산한 식별값을 사용한다. 이는 원문 저장을 줄이는 조치이지 완전한 익명화라는 뜻은 아니다. 서버가 확인한 IP만 받도록 만들며, 사용자가 보낸 임의의 헤더를 신뢰하는 연결은 이번에 추가하지 않는다.

## 검증 기록

- 시작 기준: `6f28e017c4afe47764da9519ab720b56d3d1eb5a`.
- 변경 전 전체 검사: 2,205/2,205 통과, 실패·취소·건너뜀 0.
- 기존 로컬 PostgreSQL을 `127.0.0.1:55437`에만 열어 재사용한다. 새 유료 서비스나 운영 데이터 복사본은 만들지 않는다.
- 기존 계정별 제한의 실제 PostgreSQL 재검사 2/2 통과: 서로 다른 DB 연결에서 잠금 대기, 20건 중 정확히 10건 허용, 롤백 후 복원을 확인했다.
- 배포 전 운영 1.47.0 확인: `/login` 200, `/orders` 307 로그인 이동, `/api/orders/page`와 `/api/navigation/operation-snapshot` 각각 401. 비밀번호 입력 없이 접근 보호만 확인했다.
- 구현 전 새 모듈이 없어 검사가 실패하는 것을 확인한 후 구현했다. 구현 후 신규 집중 11/11, 로그인·복구 관련 회귀 68/68, 전체 2,216/2,216 통과를 기록했다.
- 실제 PostgreSQL 신규 동시 요청 검사 3/3 통과: 같은 IP 40건 중 30건, 여러 IP 501건 중 500건만 허용했다. 서로 다른 DB 연결의 잠금 대기와 롤백 후 두 제한 복원도 확인했다.
- 전체 검사에는 기존 모듈 형식 경고와 오류 상황 검사용 진단 출력이 있다. 실패는 없지만 경고가 전혀 없는 출력으로 표현하지 않는다.
- 작업 단위 및 전체 변경 독립 검토에서 필수 수정(Critical/Important)은 없었다. 최종 검토의 보완점인 잘못된 IPv6 입력의 오류 부가 정보 노출도 수정했다. 해당 실패 재현 1건 → 수정 후 통과, 관련 21/21 통과 후 재검토에서 해결을 확인했다. 새 회귀 시험은 오류 메시지뿐 아니라 직렬화 결과와 자체 속성까지 원문이 남지 않는지 검사한다.
- 첫 빌드는 이전 `.next/static` 생성 폴더 정리 중 Windows `EPERM`으로 실패했다. 소스 수정 없이 이전 캐시를 보존하고 새 캐시로 빌드해 성공했다. 최종 보완 후 빌드도 성공했으며 최종 전체 2,217/2,217, 실패·취소·건너뜀 0을 확인했다.
- 이번 검사에서 생성한 임시 DB가 모두 삭제된 것을 확인하고 로컬 PostgreSQL을 정상 종료했다. 기존 `moaon_test_supervisor` DB 및 PostgreSQL 설치 파일은 변경하지 않았다. 운영 자료는 복사하지 않았다.

### 재현 명령과 구분

| 검사 | 명령 | 결과 |
| --- | --- | --- |
| 신규 제한·기존 로그인/복구 | `node --test test/tenant-auth-request-admission.test.js test/tenant-auth-request-limit.test.js test/tenant-fenced-login.test.js test/tenant-account-recovery.test.js` | 보완 전 68/68 |
| 최종 IP 정보 보호 보완 | `node --test test/tenant-auth-request-admission.test.js test/tenant-auth-request-limit.test.js` | 21/21 |
| 실제 PostgreSQL 동시 요청 | `node --test test/integration/tenant-auth-request-admission.native-test.js` | 3/3 |
| 기존 계정별 DB 제한 | `node --test test/integration/tenant-auth-request-limit.native-test.js` | 2/2 |
| 최종 운영용 빌드 | `pnpm build` | 성공 |
| 최종 전체 회귀 | `pnpm test` | 2,217/2,217 |

Node24.19.0, PostgreSQL17.11, Next16.3.0을 사용했다. native 명령에는 안전 검사가 있는 로컬 전용 접속 설정이 필요하며 비밀값은 문서에 기록하지 않는다. PGlite는 실제 SQL의 순차·권한·롤백 검사이고, 여러 실제 연결의 경합 검사는 native PostgreSQL에서 별도로 했다. 인증 공급자는 모의 응답으로 시험했으므로 실제 이메일 도착이나 외부 계정 가입 완료를 의미하지 않는다.

구현 커밋 `34709a1`, 최종 정보 보호 보완 `d051d8c`. 검토의 기존 모듈 경고·진단 출력 정리 권고는 별도 기술 부채로 남긴다. 이번 기능의 미해결 필수 검토 항목은 없다.

## 배포 확인

- 릴리스 태그: [v1.48.0](https://github.com/j22375646-bot/harin-food-hub/releases/tag/v1.48.0)
- 검증·배포 커밋: `2b1cb9469bac94df4fc7bccbf2f2db1ec055c4b2`. main 병합 후 같은 소스로 전체 2,217개를 다시 검사해 통과했다.
- 배포 ID: `dpl_95wxgDvCX4cW35SkJ5D4iwUKL5nA`, Vercel `READY`, production, 위 Git SHA 및 운영 도메인 연결 일치 확인.
- [운영 허브](https://harin-cafe24-sync.vercel.app), [배포 상세](https://vercel.com/j22375646-6156s-projects/harin-cafe24-sync/95wxgDvCX4cW35SkJ5D4iwUKL5nA)
- 운영 1.48.0에서 `/login` 200, `/orders` 307(`/login?next=%2Forders`), `/api/orders/page` 및 `/api/navigation/operation-snapshot` 401 확인. 네 응답 모두 버전 헤더 1.48.0. 실제 비밀번호 입력·메일 발송·외부 계정 가입은 시험하지 않았다.
- 이 배포 확인 문단은 배포 후의 문서 기록이며, 릴리스 태그와 배포 커밋은 변경하지 않는다.
- 검토 임시파일과 기존 빌드 캐시의 삭제 명령은 도구 보안 정책에 차단되어 보존했다. `.superpowers/sdd/2026-09-08-moaon-p1-04-6-auth-admission/` 및 `.next-p1046-build-cache/`는 Git·Vercel 배포에서 제외된다. 재사용 작업 디렉터리와 기존 사용자 파일도 보존했다. 로컬 파일 보존으로 새 클라우드 자원을 만들지는 않았다.

## 운영 적용과 남은 개발

기존 로그인·UI·비밀번호·채널 업무 API를 유지한다. 후보 SQL을 운영 DB에 적용하거나 새 가입 기능을 개방하지 않는다. 지금 사용할 수 있는 EXE를 만드는 단계도 아니다.

다음 인증 준비에서는 만료 제한 기록 정리·보관 정책과 아이디/이메일이 같은 계정으로 연결되는 기준을 보완한다. 이후 운영자 복구 검토·안전한 해결 절차, 실제 Auth/메일/HTTP 시험을 거쳐야 한다. 업무 자료의 사업장별 격리(P2), 각 사업장의 플랫폼 연결(P3), 앱 UI(P4), Windows EXE·프린터(P5)는 전체 계획에 그대로 남아 있다.

## 참고

- [이번 실행 계획](./2026-09-08-moaon-p1-04-6-auth-admission.md)
- [모아온 전체 개발 계획](./2026-09-07-multi-business-desktop-master-plan.md)
- [Supabase DB 함수와 권한](https://supabase.com/docs/guides/database/functions)
