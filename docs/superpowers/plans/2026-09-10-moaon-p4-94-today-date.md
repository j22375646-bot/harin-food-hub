# P4-94 — 오늘 일정 날짜 전환 / 0.82.0

## 변경
- 오늘 일정 재조회 조건에 한국 날짜 키를 추가했다. 재진입/ensure 시 날짜가 바뀌었으면 기존1분 대기와 관계없이 조회한다. 계속 열린 화면의 자정 타이머는 추가하지 않았다.
- 응답의 날짜가 표시 시점의 오늘과 다르면 목록에 넣지 않고 날짜 불일치 안내를 표시한다. 다음 ensure에서 다시 조회할 수 있도록 마지막 시도를 초기화한다.
- 조회 시작/완료/clear에 aria-busy와 버튼 상태를 맞췄다. 세션 세대 토큰/중복 요청 잠금을 유지한다.

## 검증 과정
- 테스트를 오른쪽 비포커스 bootstrap으로 이전하고 격리 IPC 응답을 사용했다. 시험 날짜/자료임을 화면에 표시한다.
- 최초 시험의 화면 초기화 누락과 시작 처리 경쟁을 수정했다. entry 준비 후 live 표시를 명시적으로 설정하여 재실행 PASS.
- 같은 날 ensure20회 추가 요청0회,날짜가 바뀌면1분 전에도1회 조회,조회 중 날짜 변경 후 이전 응답 미표시,다음 ensure에서 새 날짜 표시,HTML 안전표시/완료/오류/clear/로딩 상태 확인.
- 가상 renderer Date와 IPC 결과 시험이다. 실제 서버 자정 조회/운영 일정 대조는 아니다.
- 로그 D:/GPT/tmp/p494-ui-final.log. 공통 오늘 화면 회귀는 p494-experience.log.

## 최종 검증·배포
- 코드11개/오늘 공통 화면/소스·패키지 일정 UI PASS. 패키지 날짜 전환/늦은 응답 거부/재시도/오류/clear 검증과 캡처 확인.
- 52파일/버전/의존성 PASS. SHA256 08935bd61d7209749dcd697837c7ea2f5bd2ca50b907aae3bf943c1f678db01d.
- 로그 D:/GPT/tmp/p494-tests.log,p494-package-ui.log,p494-package-verification.json. 캡처 moaon-today-date.png.
- D:/GPT/Apps/Moaon/releases/0.82.0 배치·복사 후 검사·D:/OneDrive/바탕 화면/모아온.lnk 갱신 완료. 현재/이전/실행 중 관리형 버전 보존,사용자 앱 강제 종료 없음. 로그 p494-local-publish.log.
- 외부 자동 업데이트 주소/서명은 미설정이다.
