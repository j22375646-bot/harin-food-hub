# P4-170 일반 Gemini 대화 사용성

요청: 실무 추천 질문 30개 교체, 큰 대화 기록 화면과 개별 삭제, 원형 전송 버튼, 모델 선택, 사용량 표시. 추가 요청: 전송 시 깜빡임 제거, Flash-Lite 앱 제한을 500회로 변경, 모델 선택창 스타일 적용.

- 전송 시작/완료 때 aside 삭제 후 재생성하여 180ms 진입 애니메이션이 반복되던 원인 수정. 열린 패널 DOM은 유지하고 기존 메시지 재등장 애니메이션 제거.
- 기본 Gemini 3.5 Flash-Lite 하루 앱 한도 500회. 공개 시장 AI와 같은 모델별 원장 공유. 후속 사용자 요청으로 3.8 Flash도 앱 자체 한도 500회 적용. Google 프로젝트 한도는 미확인으로 별도 표시. 다른 모델의 500회 추정 금지. Google 잔여량 표시가 아니며 앱 KST 자정 기준임을 명시.
- model 열 및 private model reserve RPC 추가; 기존 reserve RPC는 Flash-Lite로 위임. 소유자/사업장/프로젝트 잠금, 중복 요청, 실패 차감 및 service_role 전용 권한 유지.
- 모델 선택은 키보드 조작 가능한 popover 목록. 선택 체크/호버/테두리와 밝은/어두운 테마 대응.
- 30개 추천 중 3개를 매번 열 때 교체, 직전 3개 반복 방지. 기록은 최근 저장 답변 30개 범위에서 날짜/대화 단위 카드로 표시.
- Flash 3.8의 thinkingLevel minimal 미지원 실제 오류에 따라 low 적용. 자동 모델 전환 없음.

검증: 기존 데스크톱 451개, 관련 서버/격리DB 28개 통과. 패널 동일성 회귀 검증 수정 전 실패/수정 후 통과. 패키지 117개 파일 일치. Electron 패키지 UI에서 파일 첨부, 기록/삭제, 모델 선택, 700/1040/1440 light/dark 통과. 운영 DB SQL 적용 및 Vercel READY 확인.

배포: root 1.64.4 / desktop 0.147.7. 실제 설치 후 응답 검증 결과는 후속 기록.

실제 설치 검증: 3.8 Flash / Flash-Lite 두 모델 응답 수신, 전송 전후 동일 패널 유지 확인. 마지막 후보에서는 두 모델 앱 한도 모두 500회 표시 확인. Vercel dpl_BpSihuprJ1LjxmRUkhb9M46i2TQQ READY. 최종 패키지 distribution-20260915-034618-582, 117개 파일 일치 및 패키지 UI PASS.
