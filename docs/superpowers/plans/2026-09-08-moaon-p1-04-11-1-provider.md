# P1-04-11-1 Step-Up Provider Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. User approved next development without repeat plan approval.

**Goal:** Implement the TOTP provider boundary using the real installed Supabase SDK.
**Architecture:** One request-scoped server provider with strict JWT/user/factor verification and guarded HTTP transport. No persistent hub proof yet.
**Tech Stack:** Node24, supabase-js2.55.0/auth-js2.71.1, Node crypto/test, no installs.
**Spec:** `../specs/2026-09-08-moaon-step-up-provider.md` (read completely).

## Global Constraints

- 기존 로그인·비밀번호·UI·업무 API·환경 설정·SQL은 변경하지 않는다. 새 공개 route, 운영 계정/인증 설정/메일/유료 자원, 운영 데이터 복사 금지.
- 기존 @supabase/supabase-js2.55.0/auth-js2.71.1를 사용한다. SDK 자체를 mock하지 않고 HTTP transport만 합성한다. 새 의존성 설치 없음.
- 실제 인증 완료/증거 저장·폐기 완료를 주장하지 않는다. 반환 session은 server-only 비밀이다. providerSessionId와 허브 sessionId를 혼동하지 않는다.

### Task 1: Server-only Supabase TOTP verification provider

**Files:** create `lib/tenancy/supabase-step-up-provider.js`, optional small `lib/tenancy/step-up-provider-validation.js`; create `test/tenant-supabase-step-up-provider.test.js`, `test/helpers/step-up-provider-fixture.js`. Do not edit existing business/auth/SQL files. Parent owns docs/package/changelog.

**Interfaces:** exact factory/verifyTotp/response/error/transport policy in spec. Consume installed SDK createClient only; no mock SDK or new dependency. Future authority will persist session and bind evidence, not this task. Existing auth SDK source is at `node_modules/.pnpm/@supabase+auth-js@2.71.1/node_modules/@supabase/auth-js/src/GoTrueClient.ts`. Read getClaims, setSession, _challenge, _verify before implementation; helper.js _request envelope may be needed. Test crypto keypair/JWKS stays in test helper.

- [ ] Write first behavior test. Example assertion after the controlled real SDK flow: `assert.equal(result.evidence.method,'mfa'); assert.equal(result.evidence.userId,USER); assert.equal(result.evidence.providerSessionId,SESSION); assert.equal(result.session.accessToken,verifiedJwt); assert.equal(result.evidence.verifiedAt,new Date(totpSeconds*1000).toISOString());`. Hand-build expected fixtures; assert HTTP method/path/body/Authorization and exactly one challenge+verify.
- [ ] Run `node --test test/tenant-supabase-step-up-provider.test.js`, observe intended missing feature failure, then implement minimum passing real SDK boundary.
- [ ] Add negative signed-token/input/factor tests before implementing guards. Example `await assert.rejects(()=>provider.verifyTotp(input),e=>e.code==='STEP_UP_REJECTED'); assert.equal(writes.length,0);`. Use actual ES256 signing and tampering; mock only HTTP response transport.
- [ ] Extend challenge/verify response validation and cross-user/session/old-future AMR rejection with real SDK fixtures. Assert expected literals and no secret values in error own properties/message/cause.
- [ ] Add deadline/clock/implicit-refresh/late-response/concurrent-user tests. `assert.equal(refreshRequests,0)` counts actual injected transport; invalid input does not create any network request. Verify no post-deadline new HTTP dispatch and no retries.
- [ ] Run focused suite, self-review/mutation reasoning and diff check. Commit only owned code/tests. Report RED/GREEN commands/output/counts, SDK side-effect findings and limitations in task-1-report.md. Return concise status/commit/tests/concerns.

## Parent gates

Task review and final whole-branch review, focused/full/build checks, v1.53.0 documentation, main merge/retest/push/tag/Git deployment verification. New provider stays inactive, no real account credentials/MFA enrollment or storage migration.
