# P4-104 · 이벤트 편집과 주문 뱃지 / 0.90.0

- 웹 허브 캘린더 코드의 기존 UPDATE_ENTRY를 연결했다. 모아온에서 저장된 일정/이벤트를 열고 기간·색·사은품 구간을 수정할 수 있다. 금액 구간 입력을 재배치하고 자동 판정 미리보기를 추가했다.
- Main에서 기존 항목을 조회한 뒤 확인창을 띄운다. 응답 id·이벤트 조건을 확인하고, 결과 불명 수정은 같은 id의 재전송을 잠근다. 이벤트 변경 후 주문을 재조회한다. 이벤트를 일반 일정으로 변경할 때도 주문을 재조회한다.
- 사은품 뱃지는14px로 키우고 이름·수량을 표시한다. 목록의 HR 내부 주문 식별자는 숨기고 검색/상세/실제 송장번호는 유지한다.
- 서버 shippingEstimate/timingBadge를 허용된 정보로 투영해 당일배송 대상·배송지연·출고일 확인을 표시한다. 15시 이전 및 한국 날짜를 적용한다. 다음 영업일 출고 예정 주문을 주문 당일 배송 대상으로 부르지 않는다. 송장 등록은 배송 출발 완료로 간주하지 않는다.
- 웹 허브의 기존15:00분 포함 판정과 사용자15시 이전 기준이 충돌하면 모아온에서 ‘출고일 확인’으로 남긴다. 공휴일 미확정도 지연으로 확정하지 않는다. 서버 기준 코드의 변경·배포는 이번 범위에 없다.

## 운영 읽기 확인

2026-09-11 01:44경 인증된 웹 `/calendar`, `/orders`를 조회했다. 기존 ‘테스트 사은품’ 이벤트(09-10~09-30,3만/5만 구간)가 있었고,32,600원 네이버 주문에3만원 구간의 사은품1개가 적용돼 있었다. 운영 이벤트 수정/송장 발급은 실행하지 않았다.

## 검증

- 코드343개 PASS: `D:\GPT\tmp\p4104-tests-final3.log`.
- 새 검증: UPDATE_ENTRY의 기존항목/동일id/불명 결과 재전송 차단,15:00 경계,공휴일 미확정,배송 출발 후 뱃지 제거,이벤트 금액·기간 경계와 주문 사은품 투영.
- 가상 Electron: 기존 이벤트 열기→조건 수정→확인창→수정 저장→재조회,동일 이벤트1개 유지,14px 사은품 이름·수량,목록 내부코드 숨김,당일배송 대상 표시.
- 전체 패키지 UI 결과: `D:\GPT\tmp\p4104-ui-suite\summary.json`. 설치/최종 결과는 아래에 별도로 기록한다.

## 최종 인도

- 전체 화면14개 PASS. 이후 뱃지 폭 CSS 보완 후 최종 패키지에서 주문·이벤트 화면을 다시 검증했다: `D:\GPT\tmp\p4104-final-orders.log`, `D:\GPT\tmp\p4104-final-calendar.log` 모두 PASS.
- 최종0.90.0 선언파일53개 PASS, SHA-256 `eab9d14ef16cb4259f49f2ff1d5531c1062093e462388cd628c6e02a9d126db1`.
- D드라이브 관리형 배포 및 기존 `D:\OneDrive\바탕 화면\모아온.lnk` 갱신 완료. 현재+이전+실행중 버전 보존 규칙 적용.
- 실행 중인 앱이 없어 기존 바로가기를 실행했다. 실제 프로세스43420, 경로 `D:\GPT\Apps\Moaon\releases\0.90.0\MoaonPreview.exe`, primary=false/rightOfPrimary=true 확인. 마우스·키보드 원격 조작 없음.
- 가상 화면 증거: `D:\GPT\tmp\moaon-event-edit.png`, `D:\GPT\tmp\moaon-order-design-detail.png`. 실제 운영 이벤트 수정 결과를 검증한 화면으로 제시하지 않는다.
