# P4-123 · 네이버 광고 분석과 주간 보고서 자동화 (0.109.0)

사용자 승인: 기존 분석 고도화 기획 우선 적용. 추가 요청: 최근 한 주 보고서 생성.

## 적용 범위

- 지난주 / 최근 7일 / 최근 30일. KST 기준 당일 미완료 일자는 제외한다.
- 광고비, 전환매출, 전환수, ROAS, 전환 CPA, CVR과 직전 동기간 비교. CTR/CPC/노출/클릭 보조 지표.
- 일별 광고비·전환매출 추이, 광고비-ROAS 캠페인 분포, 점 선택과 키보드 상세 열기.
- 캠페인 검색·정렬·상세, 별도 기간의 키워드 상위 자료, 자료 누락·전환 없는 집행·작은 표본 규칙 진단.
- 원천 raw 필드 누락은 null. 저장된 정상 0과 구분한다. 전환을 구매라고 바꾸지 않는다. 캠페인 자료와 다른 채널 주문 매출을 합산하지 않는다.
- 원천 60일을 안정 정렬로 페이지 조회한다. 후속 페이지 실패/상한 도달은 총액을 불완전하게 확정하지 않고 조회 불가로 처리한다.
- 서버 일일 07:00 KST 수집 → 지난 완료 주 보고서 생성/누락 보완. 동일 날짜 lease/idempotency, 보고서 동일 기간 재사용, 불완전 주 생성 보류. 이 신규 작업은 외부 알림 발송을 하지 않는다.
- 데스크톱은 기존 사업장 OWNER 전후 권한 확인을 유지하고, 제한된 읽기 전용 응답만 받는다. 오류/연결 해제 때 이전 분석을 제거한다.

## 실제 보고서

- DB에 생성: `e1360b3d-c6da-4c08-96e5-14a17d5e264b`
- 제목: 네이버 주간 자동진단 (2026-08-31~2026-09-06)
- `created: true`, 외부 알림 비활성. 앱용 실제 loader 재조회에서 해당 ID가 포함되는 것을 확인했다.
- 실제 DB 읽기: 지난주 7/7일, 캠페인 33개, 광고비 34,427원. 최근 7일은 6/7일, 최근 30일은 29/30일로 PARTIAL. 조회 시점에 전일 자료가 아직 없었다.

## 검증 및 배포 증거

- 서버 시험 2,806/2,806, 앱 코드 시험 369/369 PASS.
- 신규 계산/원천 페이지/실패/자동화/전달 계약과 기존 insights 시험 25/25 PASS.
- 신규 Electron 가상 자료 화면: 기간, 차트 2종, 캠페인 상세/검색, 오류/로그아웃, 1040/1440 너비, 밝음/어두움 PASS. 오른쪽 보조 모니터·비활성 창.
- 로컬 Next 기본 Turbopack build는 외부 node_modules junction 제한으로 실패. 같은 소스의 `next build --webpack` 성공. 운영 Vercel Turbopack build 성공.
- Vercel production READY: `dpl_Bti85snX3m1DR3Cp7zdsGnJZRNSS`, `harin-cafe24-sync-l6albdk4j-j22375646-6156s-projects.vercel.app`, 운영 alias 연결.
- Vercel 프로젝트 설정 재조회: cron 활성, `/api/cron/naver-marketing`의 일일 07:00 KST 정의가 새 배포에 연결되어 있다.
- 운영 신규 cron과 사업장 insights 경로 무인증 요청 401 확인. 인증 우회를 추가하지 않았다.
- 패키지 전체 20/20 화면 시험 PASS. `D:\GPT\tmp\p4123-suite\summary.json`.
- 0.109.0 Ed25519 업데이트 게시 완료. 실제 0.108.0 패키지 런타임의 격리 프로필에서 0.109.0 다운로드·서명 검증 PASS. 설치파일 실행은 이 시험에서 하지 않았다.
- 관리형 설치 `D:\GPT\Apps\Moaon\releases\0.109.0`, 바탕화면 모아온 바로가기 갱신 완료. 실행 중인 사용자 0.107.0은 종료하지 않았다. 사용자 앱 재시작 후 새 버전이 적용된다.
- 산출물 `desktop/dist/distribution-20260912-015239-915`, 설치파일 SHA256 `DB8ADCB073E8563C5DD1C8871B7F6047802F187BD994B3715D1F3F6260651170`.

## 명확히 남는 범위

- 예약 정의 배포와 실제 예약 실행 성공은 다르다. 로컬 일일 작업 실행은 네이버 API 환경변수 부재로 실패 기록을 남겼다. Vercel 민감 변수는 내려받아도 마스킹되므로 이를 회피하지 않았다. 새 서버 예약의 첫 성공은 아직 검증 전이다.
- 이번 수동 보고서는 DB의 확인된 지난주 저장 자료로 생성했다. 새로운 광고 원천을 실시간 재수집했다고 주장하지 않는다.
- AI 키/계정은 연결하지 않았다. 화면에 규칙 진단임을 명시한다. 무료 AI 비교와 본격 제공자 연동, 원가·재고·검색어 종합 판단, 행동 추적은 후속 단계다.
- 전체 캠페인의 날짜별 완전성은 수집일 수만으로 보장할 수 없다는 주의를 표시한다. 상위 캠페인 50개, 키워드 40개만 화면에 제공한다.
- 사용자 로그인 세션/사장님 PC의 인수 검증은 격리 자료를 이용한 패키지 UI 시험과 구분한다.
