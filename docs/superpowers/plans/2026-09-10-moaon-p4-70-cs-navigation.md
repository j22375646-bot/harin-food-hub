# P4-70 — 고객·CS 목록 탐색·상세 배치 / 0.59.0

## 변경
- 조회한 CS 목록을 25건씩 나누고 이전/다음 페이지와 페이지 수를 표시한다. 서버 조회 한도는 그대로이며 일부 조회 안내를 유지한다.
- 최신순/오래된 접수순 정렬을 추가했다. 날짜가 없는 접수는 항상 마지막에 배치한다. 검색·필터·정렬 변경은 첫 페이지로 돌아간다.
- 목록에 처리 상태를 표시한다. 상세는 목록 대신 전체 너비로 열고 상태/접수 시각을 정렬한다. 700px 이하에서는 한 열로 배치한다.
- 목록으로/Escape는 검색·필터·페이지를 유지하며 선택 접수에 초점을 돌린다.
- 서버/CS 본문/답변 이력/답변 전송은 변경하지 않았다. 기존 운영 CS 응답으로 사용할 수 있는 UI 개선이다.

## 검증
- 연결·CS 통신·서버 CS 회귀 110 PASS: D:/GPT/tmp/p470-tests.log.
- 소스 화면 19 PASS: D:/GPT/tmp/p470-ui.log.
- 패키지 화면 19 PASS: D:/GPT/tmp/p470-packaged-ui.log. 53개 가상 접수로 페이지 경계, 검색 초기화, 정렬, 날짜 누락, 상세 복귀/초점, 일부 조회/빈 결과, 680px 상세 배치를 검증했다.
- 가상 DB → 실제 CS 로더 → transport → Main IPC → DOM 시험이며 실제 운영 접수 대조는 아니다. 오른쪽 보조 모니터 right:true, focused:false. 사용자 마우스/키보드 조작 없음.
- 패키지 시험은 개발 Electron 호스트가 완성된 app.asar를 격리 프로필에서 로드했다. 설치 EXE/실제 로그인 검증과 구분한다.
- 화면: D:/GPT/tmp/moaon-cs-worklist.png.
- 폴더 빌드 exit 0, manifest 0.59.0, 앱 파일 5개 소스 바이트 일치.
- app.asar SHA256: bf0bd1bc8f83975bab4ac420d425b3609ac743452ead2b18c2d09f21a7b2d7dd.

## 전달과 남은 작업
- D:/GPT/moaon/desktop/dist/p470/win-unpacked/MoaonPreview.exe.
- 바로가기 D:/GPT/moaon/desktop/dist/p470/모아온 0.59.0 열기.lnk (--display-right). 기존 앱을 닫고 실행하며 win-unpacked 폴더 전체를 유지한다. 설치 앱은 교체하지 않았다.
- 소스·로그·스테이징·검증 프로필·빌드는 D:/GPT 아래에 저장했다.
- 이번 UI 변경은 서버 배포가 필요하지 않다. P4-66~68 데이터 확장은 운영 적용 대기이며 Vercel 한도 해제 여부를 이번에 재확인하지 않았다.
- 다음 기능은 고객·CS 본문/답변 이력 조회다. 실제 로그인 자료 대조 및 자동 업데이트 배포/서명/설치 인수도 남아 있다.
