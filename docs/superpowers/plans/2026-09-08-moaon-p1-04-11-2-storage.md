# P1-04-11-2 Step-Up Storage Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. User approved next development and no repeat approval pauses.

**Goal:** Store encrypted provider credentials and bind/revoke current hub MFA evidence.
**Architecture:** Inactive server composition with DB-reserved attempts, atomic sealed credential/proof commit and fresh read authority.
**Tech Stack:** Existing Node24 crypto, Supabase SDK, PGlite, pg. No installs.
**Spec:** ../specs/2026-09-08-moaon-step-up-storage.md

## Global Constraints

기존 로그인·비밀번호·UI·업무 API·운영 SQL·환경 설정은 바꾸지 않는다. 공개 route/실계정 MFA/메일/유료 자원/운영 자료 복사 금지. 기존 Node crypto, pg, PGlite, Supabase SDK 사용; 설치 없음. 후보 SQL은 lib/tenancy/sql 아래에 두고 운영 migration으로 자동 적용하지 않는다.

### Task 1: Atomic encrypted step-up storage and server composition

**Files:** create lib/tenancy/sql/step-up-storage.sql; lib/tenancy/step-up-storage.js; optional lib/tenancy/step-up-seal.js and lib/tenancy/step-up-storage-validation.js. Tests create test/tenant-step-up-storage.test.js, test/helpers/step-up-storage-fixture.js, test/integration/tenant-step-up-storage.native-test.js. Parent owns all docs/version. Do not edit existing source/provider/helpers/migrations without raising a concrete conflict.

**Interfaces:** Exact RPC/createStepUpStorage signatures and structures are in the spec. Read it completely. Existing reference auth-session-fence.sql lock order, dashboard migration, supabase-step-up-provider.js, test/helpers/step-up-provider-fixture.js, test/helpers/recovery-request-fixture.js; native safety harness test/integration/tenant-recovery-admission.native-test.js. Test-only auth.sessions/mfa_factors fixtures must contain only synthetic accounts and specified columns. No hosted DB operations. Parent manages native Postgres process and full suite/build/release.

- [ ] Add first PGlite behavior test, fail missing SQL/module, implement minimal begin/commit/read/revoke. Example assertions: assert.deepEqual(await service.verifyStepUp({userId,sessionId}),expectedProof); assert.equal(rows[0].sealed_session.ciphertext.includes(refreshToken),false); assert.equal(rows[0].state,'VERIFIED'). Run node --test test/tenant-step-up-storage.test.js and record actual RED/GREEN.
- [ ] Add failure scenarios before guards: wrong hash/user/session, revoked session, account generation, pending expiration, provider session/factor absent, wrong factor, infinity/future timestamps, raw SQL null/extra malformed envelope; assert rejection/null and unchanged rows. Show server-role-only RPC/table grants and repeated install preserves data.
- [ ] Implement exact-key server issue/read/load/revoke composition and AES-GCM seal boundary with failing literal fixture tests first. assert.deepEqual(await service.loadSession(identity),{accessToken,refreshToken}); assert.equal(rpcArgumentsContainRawSecrets,false). Prove tampered cipher/tag/AAD/key fails without secrets and complete native string/type/getter validation before I/O.
- [ ] Add vertical real SDK + ES256 fixture + PGlite tests, not mock SDK. assert proof.sessionId===hubSession and differs from providerSessionId. Reserve→provider→commit sequence, begin/commit fail/pending timeout and no retries, expired result before dispatch, late RPC/provider no follow-up. Auth response/token state must stay local. Existing recovery verifyStepUp input/shape compatibility literal assertion.
- [ ] Add native multi-client tests (excluded default glob, explicit loopback opt-in) for concurrent begin one approval, commit vs revoke both serialization orders, password-reset/account generation race; use actual lock barriers and assert ciphertext absent/late commit denied. Parent executes native suite after implementation.
- [ ] Focused tests and syntax/diff/self-review, commit ONLY owned code/tests (never force add .superpowers). Full report to task-1-report.md includes exact RED/GREEN commands, counts/output, schema grants, lock ordering, timeout/ambiguous outcomes, remaining activation gates. No subagents. Return short status/commit/tests/report.

## Parent gates

Worktree baseline, task review/fix loop, native DB execution/cleanup, full tests/build, final whole-branch review, main retest/push/tag1.54.0 and READY/version/protected-route checks. Preserve unrelated user files and current production auth.
