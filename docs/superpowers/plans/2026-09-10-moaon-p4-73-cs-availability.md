# P4-73 — 고객·CS 조회 상태·검색 안내 / 0.62.0

## 변경
- 검색 범위 안내를 긴 목록 아래에서 검색창과 목록 사이로 이동했다.
- 조회 전/조회 중/성공 0건/구 서버/본문 응답을 구분한다. 접수가 없거나 조회 실패한 상황을 서버 미반영으로 잘못 표시하지 않는다.
- 목록에 제목과 저장 내용 조회됨/일부 내용 조회됨/저장 내용 확인 필요/복원 실패/본문 연결 대기를 표시한다.
- 조회된 전체 자료에 대해 내용 조회·확인 필요·연결 대기 건수를 요약한다. 조회 실패·로그아웃 초기화 시 요약을 숨긴다. 숫자는 현재 검색 조건 건수와 별도다.
- 서버 변경 없음. 기존 서버에서도 번호 검색 안내와 연결 대기 표시가 가능하다. 본문·제목·이력은 P4-71 서버 적용 후 표시된다.

## 검증
- 관련 코드 시험 115 PASS: D:/GPT/tmp/p473-tests.log.
- 소스 화면 36 PASS: D:/GPT/tmp/p473-ui.log. 제목/복원 실패·가용 건수·검색 안내 위치·실패 시 요약 제거·성공 0건의 안내를 추가로 검증했다.
- 가상 자료 → 실제 서버 로더 → transport → Main IPC → DOM이며 실제 로그인 자료 대조는 아니다. 오른쪽 보조 모니터 right:true, focused:false.
- 목록 화면 D:/GPT/tmp/moaon-cs-availability.png, 상세 화면 D:/GPT/tmp/moaon-cs-worklist.png.

## 운영과 다음 범위
- P4-72에서 Vercel 일일 배포 제한을 재확인했으므로 이번 UI 단계에서는 배포를 재시도하지 않았다. 제한 해제 여부는 미확인이다.
- P4-66~68/P4-71 서버 적용·실자료 대조, 완료 접수 이력 조회는 미완료다.
- 개발/로그/스테이징/시험 프로필/패키지는 D:/GPT에 저장하며 기존 설치·실행 앱을 교체하지 않는다.

## 최종 패키지
- 빌드 exit 0: D:/GPT/tmp/p473-package.log, manifest 0.62.0, 앱 파일 5개 소스 바이트 일치.
- app.asar SHA256: 0c6befd35114e654b3ea9aa2de02530aeb08396fda2584070be850d9212ceb40.
- 패키지 화면 36 PASS: D:/GPT/tmp/p473-packaged-ui.log, right:true/focused:false. 개발 Electron 호스트가 완성된 app.asar를 격리 프로필에서 로드했다.
- 새 목록 캡처를 직접 확인했다. 검색 안내는 검색창 아래에 있고 제목/내용 상태와 페이지 도구가 표시된다.
- 실행: D:/GPT/moaon/desktop/dist/p473/모아온 0.62.0 열기.lnk (--display-right). 기존 앱을 닫고 실행하며 win-unpacked 폴더 전체를 유지한다.
