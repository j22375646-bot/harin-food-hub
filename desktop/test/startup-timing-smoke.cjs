// Isolated local shell timing; excludes authenticated network/data readiness.
const fs=require('node:fs'),path=require('node:path'),os=require('node:os'),{_electron}=require('playwright');
(async()=>{const rows=[];for(let i=0;i<3;i++){const start=performance.now();const app=await _electron.launch({executablePath:require('electron'),args:[path.join(__dirname,'isolated-bootstrap.cjs')],env:{...process.env,MOAON_TEST_RUNTIME_ROOT:process.env.MOAON_STARTUP_RUNTIME||path.resolve(__dirname,'..'),MOAON_TEST_PROFILE:fs.mkdtempSync(path.join(os.tmpdir(),'startup-')),MOAON_TEST_HIDDEN:'0',MOAON_TEST_DISPLAY:'main'}});try{const page=await app.firstWindow();await page.waitForLoadState('load');await page.waitForFunction(()=>Boolean(window.moaonHub)&&Boolean(document.querySelector('#entry-login')));rows.push({shellMs:Math.round(performance.now()-start),navigation:await page.evaluate(()=>({domMs:Math.round(performance.getEntriesByType('navigation')[0].domContentLoadedEventEnd),loadMs:Math.round(performance.getEntriesByType('navigation')[0].loadEventEnd)}))});}finally{await app.close();}}console.log(JSON.stringify(rows));})().catch(e=>{console.error(e);process.exitCode=1;});
