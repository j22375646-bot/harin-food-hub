'use strict';
const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),{_electron}=require('playwright');
(async()=>{const app=await _electron.launch({executablePath:require('electron'),args:[path.join(__dirname,'isolated-bootstrap.cjs')],env:{...process.env,MOAON_TEST_RUNTIME_ROOT:process.env.MOAON_TEAM_RUNTIME||path.resolve(__dirname,'..'),MOAON_TEST_PROFILE:fs.mkdtempSync('D:/GPT/tmp/team-ui-'),MOAON_TEST_HIDDEN:'0',MOAON_TEST_DISPLAY:'main'}});try{const page=await app.firstWindow(),errors=[];page.setDefaultTimeout(15000);page.on('pageerror',e=>errors.push(e.message));await page.waitForLoadState('domcontentloaded');await page.evaluate(()=>runHubAction('disconnect'));await app.evaluate(({session})=>{const ids=['10000000-0000-4000-8000-000000000001','10000000-0000-4000-8000-000000000002','10000000-0000-4000-8000-000000000003'];globalThis.teamFixture={me:ids[0],members:ids.map((id,i)=>({id,name:['검증 직원','검증 사장','검증 부사장'][i],title:'자동 검증',color:'violet',notifications:false,revision:1})),tasks:[],events:[]};session.fromPartition('persist:moaon-harin-readonly').fetch=async(url,options)=>{const f=globalThis.teamFixture;if(url.endsWith('/api/moaon/team')){if(options.method==='POST'){const v=JSON.parse(options.body);if(v.action==='CREATE')f.tasks.push({id:v.id,title:v.title,notes:v.notes,due_date:v.dueDate,assigned_to:v.assignedTo,created_by:f.me,checklist:v.checklist,status:'OPEN',revision:1});else if(v.action==='PROFILE'){Object.assign(f.members[0],{name:v.name,title:v.title,color:v.color,notifications:v.notifications,revision:2});}else{const t=f.tasks.find(t=>t.id===v.id);if(v.action==='CHECK')t.checklist[v.index].done=v.done;else t.status=v.action==='COMPLETE'?'DONE':'OPEN';t.revision++;}return Response.json({ok:true,value:{saved:true}});}return Response.json({ok:true,value:f});}return Response.json({ok:true,orders:[],total:0,offset:0,nextOffset:null,snapshot:'a'.repeat(64),partial:false});};});await page.evaluate(()=>runHubAction('viewActive'));await page.waitForFunction(()=>document.querySelector('#team-today').textContent.includes('검증 직원'));await page.evaluate(()=>showRoute('calendar'));await page.locator('#team-board').getByRole('button',{name:'+ 업무 지시',exact:true}).click();const dialog=page.getByRole('dialog',{name:'업무 등록 및 처리'});await dialog.getByLabel('업무 제목',{exact:true}).fill('자동 검증 · 출고 준비');await dialog.getByLabel('체크리스트 · 한 줄에 하나씩').fill('수량 확인\n포장 확인');await dialog.getByRole('button',{name:'업무 등록',exact:true}).click();await dialog.waitFor({state:'hidden'});assert.equal(await page.locator('#team-board .team-task').count(),1);await page.locator('#team-board .team-check input').first().check();await page.waitForFunction(()=>document.querySelector('#team-board .team-check input').checked);await page.locator('#team-board .team-task-top input').check();await page.waitForFunction(()=>document.querySelector('#team-board .team-task').classList.contains('is-done'));await page.evaluate(()=>showRoute('today'));assert.equal(await page.locator('#team-today .team-task').count(),0);await page.locator('#business-settings-open').click();const profile=page.getByRole('dialog',{name:'내 프로필 설정'});await profile.getByLabel('표시 이름',{exact:true}).fill('검증 내 이름');await profile.getByRole('button',{name:'프로필 저장'}).click();await profile.waitFor({state:'hidden'});await page.waitForFunction(()=>document.querySelector('#business-settings-open').title.includes('검증 내 이름'));for(const width of [1040,1440]){await app.evaluate(({BrowserWindow},w)=>BrowserWindow.getAllWindows()[0].setSize(w,980),width);for(const route of ['today','calendar','settings']){await page.evaluate(r=>showRoute(r),route);await page.waitForTimeout(300);assert.equal(await page.locator('.page:not([hidden])').evaluate(e=>e.scrollWidth<=e.clientWidth),true);}}await page.evaluate(()=>showRoute('calendar'));await page.waitForTimeout(600);const png=await app.evaluate(async({BrowserWindow})=>(await BrowserWindow.getAllWindows()[0].webContents.capturePage()).toPNG().toString('base64'));fs.writeFileSync('D:/GPT/tmp/p4129-team.png',Buffer.from(png,'base64'));await page.evaluate(()=>runHubAction('disconnect'));assert.equal(await page.locator('#team-board .team-task').count(),0);assert.deepEqual(errors,[]);console.log('PASS visible main: create/check/complete/shared today/calendar/profile/responsive/logout; synthetic data only');}finally{await app.close();}})().catch(e=>{console.error(e);process.exitCode=1});
