'use strict';
const test=require('node:test');const assert=require('node:assert/strict');
const policy=require('../connection-policy.cjs');const {projectOrdersPayload}=require('../hub-connection.cjs');
test('canonical search and export URLs remain tenant-bound',()=>{const search={delayOnly:false,giftOnly:false,query:'김 스낵',start:'2026-09-01',end:'2026-09-09'};const url=policy.buildOrdersScopeUrl('ACTIVE','CAFE24',search);assert.match(url,/query=%EA%B9%80%20%EC%8A%A4%EB%82%B5&start=2026-09-01&end=2026-09-09$/);assert.equal(policy.buildOrdersExportUrl('ACTIVE','CAFE24',search),url+'&format=xlsx');assert.equal(policy.isAllowedRemoteRequest({method:'GET',url,webContentsId:0}),true);assert.equal(policy.isAllowedRemoteRequest({method:'GET',url:url+'&evil=1',webContentsId:0}),false);});
test('searched page requires declared contract metadata',()=>{const base={ok:true,orders:[],total:0,offset:0,nextOffset:null,snapshot:'a'.repeat(64),partial:false};assert.throws(()=>projectOrdersPayload(base,'2026-09-09T00:00:00Z',{requireSearchContract:true}));const result=projectOrdersPayload({...base,searchContractVersion:1,appliedSearch:{query:'김',start:'',end:''}},'2026-09-09T00:00:00Z',{requireSearchContract:true});assert.equal(result.search.query,'김');});
