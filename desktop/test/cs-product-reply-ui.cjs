const assert=require('node:assert/strict'),fs=require('fs'),path=require('path'),{_electron}=require('playwright');
(async()=>{const app=await _electron.launch({executablePath:require('electron'),args:[path.join(__dirname,'isolated-bootstrap.cjs')],env:{...process.env,MOAON_TEST_RUNTIME_ROOT:process.env.MOAON_TEST_RUNTIME_ROOT||path.resolve(__dirname,'..'),MOAON_TEST_PROFILE:fs.mkdtempSync('D:/GPT/tmp/cs200-'),MOAON_TEST_HIDDEN:'0',MOAON_TEST_DISPLAY:'right'}});try{const p=await app.firstWindow(),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.waitForLoadState('domcontentloaded');await app.evaluate(({ipcMain})=>{globalThis.sent=0;ipcMain.removeHandler('moaon-hub:read-cs');ipcMain.handle('moaon-hub:read-cs',()=>({status:'READY',generatedAt:new Date().toISOString(),truncated:false,items:[{id:'COUPANG:INQUIRY:ONLINE:123',platform:'COUPANG',kind:'INQUIRY',status:'미답변',occurredAt:new Date().toISOString(),details:{status:'AVAILABLE',title:'',body:'재입고 문의',history:[],truncated:false,updatedAt:new Date().toISOString(),product:{name:'검증용 작두콩수세미차',productId:'1234',sellerProductId:'5678',vendorItemId:'999'},reply:{available:true,sourceUpdatedAt:'2026-09-16T00:00:00Z'}}}]}));ipcMain.removeHandler('moaon-hub:team-command');ipcMain.handle('moaon-hub:team-command',(_,v)=>{if(v.action==='CS_READ')return {ok:true,value:{revision:0,config:null}};if(v.action==='CS_REPLY_SEND'){globalThis.sent++;return {ok:true,value:{status:'PENDING',requestId:'test'}};}return {ok:true,value:{status:globalThis.sent?'PENDING':'NONE',requestId:null}};});});await p.evaluate(()=>{displayMode='live';document.querySelector('#entry-screen').hidden=true;document.querySelector('.preview-shell').hidden=false;document.querySelector('.preview-shell').inert=false;showRoute('cs');});await p.waitForFunction(()=>document.querySelectorAll('.cs-row').length===1);assert.equal(await p.locator('#nav-cs-count').innerText(),'1');await p.locator('.cs-row').evaluate(n=>n.click());await p.waitForFunction(()=>document.querySelector('#cs-detail').textContent.includes('아직 모아온'));assert.match(await p.locator('#cs-detail').innerText(),/검증용 작두콩수세미차/);await p.getByLabel('이번 문의 답변',{exact:true}).fill('자동 검증용 답변입니다.');await p.getByLabel('쿠팡 Wing 로그인 ID',{exact:true}).fill('fixture');await p.getByRole('button',{name:'쿠팡에 답변 보내기',exact:true}).evaluate(n=>n.click());assert.equal(await app.evaluate(()=>globalThis.sent),0);await p.getByRole('button',{name:'확인 · 실제 전송',exact:true}).evaluate(n=>n.click());await p.waitForFunction(()=>document.querySelector('#cs-detail').textContent.includes('전송 대기'));assert.equal(await app.evaluate(()=>globalThis.sent),1);assert.equal(await p.getByRole('button',{name:'쿠팡에 답변 보내기',exact:true}).isDisabled(),true);assert.deepEqual(errors,[]);await p.screenshot({path:'D:/GPT/tmp/p4200-cs.png'});console.log('PASS counter, product context, explicit reply confirmation and duplicate lock');}finally{await app.close();}})().catch(e=>{console.error(e.message);process.exitCode=1});
