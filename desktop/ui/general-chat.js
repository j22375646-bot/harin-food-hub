'use strict';
(()=>{
 const $=id=>document.getElementById(id),el=(tag,cls,text)=>{const n=document.createElement(tag);if(cls)n.className=cls;if(text!==undefined)n.textContent=text;return n;},button=(text,fn)=>{const n=el('button','',text);n.type='button';n.onclick=fn;return n;};
 let opened=false,busy=false,loading=false,loaded=false,epoch=0,turns=[],reports=[],selected=[],files=[],draft='',conversationId=null,configuration=null,status='',animation=0,pending='',newAnswer=null;
 const suggestionPool=[["오늘 업무 우선순위 정리","오늘 해야 할 일을 적어줄게. 주문·고객 응대·재고·마케팅으로 나누고 급한 순서로 정리해 줘. 먼저 할 일 목록을 물어봐 줘."],["하루 마감 체크리스트","식품 온라인몰의 하루 마감 체크리스트를 만들어 줘. 미처리 주문, 송장, 고객 문의, 재고를 빠뜨리지 않게 정리해 줘."],["이번 주 운영 계획 세우기","이번 주 쇼핑몰 운영 계획을 같이 세워 줘. 행사 일정과 해야 할 일을 먼저 질문하고 하루별 실행 계획을 제안해 줘."],["반복 업무 줄이는 방법","내 반복 업무를 줄일 방법을 찾아줘. 어떤 일을 얼마나 자주 하는지 먼저 물어보고 자동화할 일과 체크리스트로 만들 일을 나눠줘."],["업무 인수인계 문서 만들기","주문 처리와 고객 응대 업무를 다른 담당자에게 넘길 인수인계 문서 틀을 만들어 줘. 담당자, 처리 기준, 예외 상황을 포함해 줘."],["배송 지연 안내문 작성","배송이 늦어지는 고객에게 보낼 안내문을 작성해 줘. 지연 이유와 예상 출고일을 먼저 물어보고, 과장 없이 정중하게 써 줘."],["교환·반품 답변 다듬기","고객의 교환·반품 문의에 답하려고 해. 문의 내용과 우리 처리 기준을 먼저 확인하고, 확정되지 않은 약속 없이 답변 초안을 만들어 줘."],["상품 보관법 안내문","차·식품의 보관법을 고객에게 쉽게 안내하고 싶어. 상품 표시사항을 먼저 확인하고 확인된 내용으로만 짧은 안내문을 써 줘."],["반복 문의를 FAQ로","반복되는 고객 문의를 붙여넣을게. 개인정보를 제외한 내용을 질문·짧은 답변·추가 확인 항목으로 정리해 줘."],["불만 고객 답변 검토","불만 고객에게 보낼 답변을 검토해 줘. 공감은 하되 확인되지 않은 책임이나 보상은 단정하지 않도록 다듬어 줘."],["묶음 상품 구성 아이디어","차 상품으로 묶음 구성을 만들고 싶어. 판매 중인 상품과 가격, 고객층을 먼저 물어보고 목적별 구성안을 제안해 줘."],["사은품 행사 조건 정리","사은품 행사를 준비하려고 해. 증정 조건, 준비 수량, 소진 시 안내, 중복 적용 기준을 질문하고 운영 체크리스트로 정리해 줘."],["상품명 더 읽기 쉽게","상품명을 붙여넣을게. 제품 종류·용량·수량이 잘 보이도록 정리하고, 확인되지 않은 효능이나 인증 문구는 추가하지 마."],["상세페이지 개선점 찾기","상세페이지 문구나 이미지를 첨부할게. 첫 화면, 상품 정보, 구매 전 궁금증, 신뢰 근거 순서로 개선점을 정리해 줘."],["상품 옵션 간단히 정리","상품 옵션을 간단하고 헷갈리지 않게 정리하고 싶어. 현재 옵션을 물어보고 용량·구성·총수량이 구분되는 이름을 제안해 줘."],["재고 발주 기준 세우기","재고 발주 기준을 세우고 싶어. 현재 재고, 최근 판매량, 입고 소요일을 먼저 물어보고 부족한 정보와 계산 기준을 구분해 줘."],["유통기한 임박 상품 계획","유통기한이 임박한 상품의 운영 계획을 세워 줘. 품목, 수량, 기한, 원가를 먼저 물어보고 할인·묶음·노출 방안을 비교해 줘."],["입고 검수 체크리스트","차·식품 입고 검수 체크리스트를 만들어 줘. 수량, 파손, 표시사항, 소비기한과 유통기한, 로트 기록을 포함해 줘."],["재고 차이 원인 점검","장부 재고와 실제 재고가 달라. 입출고 기록과 실사 내용을 먼저 물어보고 확인할 원인을 순서대로 정리해 줘."],["품절 안내와 대체 상품","품절 안내문과 대체 상품 제안 문구를 작성해 줘. 재입고 일정과 실제 대체 가능한 상품을 먼저 확인해 줘."],["행사 아이디어 비교하기","다음 행사의 아이디어를 비교해 줘. 목표, 대상 고객, 예산, 기간을 먼저 물어보고 실행 난이도와 확인할 지표를 함께 정리해 줘."],["할인해도 남는지 계산","할인 행사의 손익을 검토하고 싶어. 판매가, 원가, 수수료, 배송비, 할인 조건을 먼저 물어보고 빠진 비용은 확인 필요로 표시해 줘."],["마케팅 메시지 초안","고객에게 보낼 마케팅 메시지 초안을 작성해 줘. 실제 혜택과 행사 기간을 먼저 물어보고 짧은 버전과 자세한 버전을 제안해 줘."],["명절 선물세트 기획","명절 선물세트를 기획하고 싶어. 예산대, 상품 구성, 포장, 주문 마감일을 질문하고 준비 일정까지 정리해 줘."],["행사 후 회고 정리","행사가 끝난 뒤 회고를 작성하려고 해. 목표와 실제 결과를 먼저 물어보고 잘된 점, 아쉬운 점, 다음 행동으로 정리해 줘."],["보고서 핵심만 요약","선택하거나 첨부한 보고서가 있으면 핵심을 요약해 줘. 관찰된 사실, 추정, 추가 확인 항목을 구분하고 자료가 없으면 먼저 요청해 줘."],["광고비 점검 항목","네이버 광고비를 점검하고 싶어. 기간과 광고 성과 자료를 먼저 물어보고 낭비 가능성과 추가 확인할 항목을 정리해 줘."],["엑셀 수식 도움받기","주문·재고 정리에 쓸 엑셀 수식을 도와줘. 열 이름과 원하는 결과를 먼저 물어보고 예시 수식과 사용법을 알려줘."],["회의 메모를 할 일로","회의 메모를 붙여넣을게. 결정 사항, 담당자별 할 일, 기한, 미정 사항으로 나눠 정리해 줘."],["두 선택지 장단점 비교","운영상 두 가지 선택지 사이에서 고민 중이야. 선택지, 목표, 제약을 먼저 질문하고 비용·시간·고객 영향으로 비교해 줘."]];
 let suggestionSet=[],historyOpen=false,model='gemini-3.5-flash-lite',quota=null,quotas=[];
 function shuffleSuggestions(){const rest=suggestionPool.filter(s=>!suggestionSet.includes(s));for(let i=rest.length-1;i>0;i--){const j=crypto.getRandomValues(new Uint32Array(1))[0]%(i+1);[rest[i],rest[j]]=[rest[j],rest[i]];}suggestionSet=rest.slice(0,3);}
 shuffleSuggestions();
 const uuid=()=>crypto.randomUUID();
 const errors={DISABLED:'Gemini 사용이 꺼져 있어요. 앱 설정에서 확인해 주세요.',SETUP_REQUIRED:'Gemini 연결 설정을 확인해 주세요.',QUOTA_BLOCKED:'현재 사용 한도에 도달했어요. 잠시 뒤 다시 시도해 주세요.',TIMEOUT:'응답 시간이 길어졌어요. 질문은 유지됩니다.',LOGIN_REQUIRED:'로그인 후 Gemini를 사용할 수 있어요.',FORBIDDEN:'사업장 접근 권한을 확인해 주세요.',BLOCKED:'선택한 자료를 확인할 수 없어요. 선택을 해제하거나 다시 조회해 주세요.',INVALID_OUTPUT:'답변을 받지 못했어요. 질문을 바꿔 다시 시도해 주세요.',CANCELLED:'대기를 취소했어요.',ALREADY_PROCESSED:'이미 처리된 요청입니다. 대화 기록을 다시 확인해 주세요.'};
 const message=r=>errors[r?.status]||'연결을 확인한 뒤 다시 시도해 주세요.';
 const launcher=button('✦ Gemini에게 물어보기',()=>opened?close():open());launcher.id='general-chat-launcher';launcher.className='insight-ai-launcher';launcher.setAttribute('aria-controls','general-chat-panel');document.querySelector('.chrome-actions').prepend(launcher);
 function capture(){const input=$('general-chat-question');if(input&&!busy)draft=input.value;}
 function close(){capture();opened=false;render();launcher.focus({preventScroll:true});}
 function clear(){historyOpen=false;quota=null;quotas=[];epoch++;void window.moaonHub.cancelGeneralChat();opened=false;busy=false;loading=false;loaded=false;turns=[];selected=[];reports=[];files=[];draft='';pending='';conversationId=null;configuration=null;status='';render();}
 function open(){shuffleSuggestions();window.moaonInsightAI?.closeQuestion();opened=true;render();window.moaonInsights?.ensure();if(!busy&&!loading)void load();}
 async function load(){if(loading||busy)return;loading=true;const e=epoch;render();try{const r=await window.moaonHub.generalChat({operation:'LIST'});if(e!==epoch)return;if(r.ok){configuration=r.configuration;quota=r.quota;quotas=r.quotas?.length?r.quotas:[r.quota];turns=r.turns;loaded=true;if(!conversationId&&turns.length){conversationId=turns[0].conversationId;selected=[...turns[0].reportIds];}status=configuration.ready&&configuration.enabled?'':message(configuration);}else status=message(r);}catch{if(e===epoch)status=message();}finally{if(e===epoch){loading=false;render();}}}
 async function send(){capture();if(busy||loading||!draft.trim())return;conversationId=conversationId||uuid();const question=draft.trim(),e=epoch,input={requestId:uuid(),conversationId,question,reportIds:[...selected],files:[...files],model};busy=true;pending=question;draft='';status='';render();try{const r=await window.moaonHub.generalChat({operation:'GENERATE',input});if(e!==epoch)return;if(r.ok){quota=r.quota;quotas=[r.quota,...quotas.filter(q=>q?.model!==r.quota?.model)];turns=[r.turn,...turns.filter(t=>t.id!==r.turn.id)];newAnswer=r.turn.id;files=[];status='';}else{quota=null;quotas=quotas.filter(q=>q?.model!==model);draft=question;status=message(r);}}catch{if(e===epoch){draft=question;status=message();}}finally{if(e===epoch){busy=false;pending='';render();}}}
 async function attach(list){capture();const e=epoch;for(const file of Array.from(list)){const ext=file.name.split('.').pop().toLowerCase(),mime=({png:'image/png',jpg:'image/jpeg',jpeg:'image/jpeg',pdf:'application/pdf',txt:'text/plain',md:'text/plain',csv:'text/plain'})[ext];if(!mime){status='이미지(PNG·JPG), PDF, TXT·MD·CSV 파일을 선택해 주세요.';break;}if(files.length>=3||files.reduce((n,f)=>n+f.data.length*3/4,0)+file.size>2*1024*1024){status='파일은 최대 3개, 합계 2MB까지 첨부할 수 있어요.';break;}if(!file.size){status='빈 파일은 첨부할 수 없어요.';break;}const data=await new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(String(r.result).split(',')[1]);r.onerror=reject;r.readAsDataURL(file);});if(e!==epoch)return;files.push({name:file.name.slice(0,160),mimeType:mime,data});}render();}
 function newChat(){if(busy)return;historyOpen=false;shuffleSuggestions();conversationId=uuid();draft='';files=[];selected=[];status='';render();}
 function deleteChat(id=conversationId){if(busy||!id)return;const wasHistory=historyOpen,d=el('dialog','insight-ai general-chat-confirm');d.setAttribute('aria-label','대화 삭제 확인');d.append(el('h2','','이 대화를 삭제할까요?'),el('p','','이 대화의 질문, 답변과 첨부파일을 삭제합니다.'));const no=button('취소',()=>d.close()),yes=button('대화 삭제',async()=>{d.close();const e=epoch;busy=true;render();try{const r=await window.moaonHub.generalChat({operation:'DELETE',conversationId:id});if(e!==epoch)return;if(r.ok){turns=turns.filter(t=>t.conversationId!==id);busy=false;if(conversationId===id)newChat();historyOpen=wasHistory;}else status=message(r);}catch{if(e===epoch)status=message();}finally{if(e===epoch){busy=false;render();}}});const actions=el('div','general-chat-confirm-actions');yes.className='general-chat-confirm-delete';actions.append(no,yes);d.append(actions);d.onclose=()=>d.remove();document.querySelector('.app-area').append(d);d.showModal();no.focus({preventScroll:true});}
 function greeting(){const name=document.querySelector('.settings-profile-identity strong')?.textContent;return name&&!['내 계정','프로필'].includes(name)?name+'님, 안녕하세요':'안녕하세요';}
 function render(){
  cancelAnimationFrame(animation);launcher.setAttribute('aria-expanded',String(opened));const focused=document.activeElement?.id==='general-chat-question';const existing=$('general-chat-panel');if(!opened){existing?.remove();return;}
  const panel=existing||el('aside','insight-ai insight-ai-chat general-chat');panel.replaceChildren();panel.id='general-chat-panel';panel.setAttribute('aria-labelledby','general-chat-title');const head=el('header','insight-ai-chat-heading'),title=el('h2','',historyOpen?'대화 기록':'Gemini와 대화');title.id='general-chat-title';head.append(title,button('닫기',close));panel.append(head);
  const tools=el('div','insight-ai-chat-tools');const fresh=button('새 대화',newChat);fresh.disabled=busy;tools.append(fresh);const history=button(historyOpen?'대화로 돌아가기':'대화 기록',()=>{capture();historyOpen=!historyOpen;render();});tools.append(history);if(!historyOpen&&turns.some(t=>t.conversationId===conversationId)){const del=button('대화 삭제',()=>deleteChat());del.disabled=busy;tools.append(del);}panel.append(tools);
  if(historyOpen){const list=el('div','general-chat-history');list.append(el('p','general-chat-help','최근 저장된 답변 30개 · 대화를 선택하면 이어서 볼 수 있어요.'));const groups=new Map();for(const t of turns){if(!groups.has(t.conversationId))groups.set(t.conversationId,[]);groups.get(t.conversationId).push(t);}let previousDay='';for(const [id,group] of groups){const latest=group[0],first=group[group.length-1],day=new Date(latest.createdAt).toLocaleDateString('ko-KR');if(day!==previousDay){list.append(el('h3','',day));previousDay=day;}const card=el('article','general-chat-history-card'+(id===conversationId?' is-current':''));const choose=button('',()=>{conversationId=id;selected=[...latest.reportIds];model=latest.model;draft='';files=[];historyOpen=false;status='';render();});choose.disabled=busy;choose.append(el('strong','',first.question.slice(0,80)),el('p','',latest.answer.slice(0,130)),el('small','',group.length+'개 답변 · '+new Date(latest.createdAt).toLocaleTimeString('ko-KR',{hour:'2-digit',minute:'2-digit'})));card.append(choose);const del=button('삭제',()=>deleteChat(id));del.setAttribute('aria-label',first.question.slice(0,40)+' 대화 삭제');del.disabled=busy;card.append(del);list.append(card);}if(!groups.size)list.append(el('p','','아직 저장된 대화가 없어요. 새로운 대화를 시작해 보세요.'));panel.append(list);if(!panel.isConnected)document.querySelector('.app-area').append(panel);return;}

  const picker=el('details','insight-ai-picker');picker.append(el('summary','',selected.length?'참고 자료 '+selected.length+'개 선택':'자료 선택 · 선택하지 않아도 대화할 수 있어요'));const choices=el('fieldset','insight-ai-scope');choices.append(el('legend','','선택한 보고서만 Gemini에 함께 보냅니다'));
  for(const report of reports){const label=el('label','insight-ai-choice'),input=el('input');input.type='checkbox';input.checked=selected.includes(report.id);input.disabled=busy||!input.checked&&selected.length>=2;input.onchange=()=>{capture();selected=input.checked?[...selected,report.id]:selected.filter(id=>id!==report.id);render();};label.append(input,el('span','',report.title+' · '+report.periodStart.slice(0,10)+' — '+report.periodEnd.slice(0,10)));choices.append(label);}if(!reports.length)choices.append(el('p','','선택할 보고서를 조회 중이거나 저장된 보고서가 없습니다.'));const reset=button('자료 선택 해제',()=>{capture();selected=[];render();});reset.disabled=busy;choices.append(reset);picker.append(choices);panel.append(picker);
  const log=el('div','insight-ai-transcript');log.setAttribute('role','log');log.setAttribute('aria-label','Gemini 대화');log.tabIndex=0;const chain=turns.filter(t=>t.conversationId===conversationId).slice().reverse();
  function bubble(role,text){const b=el('article','insight-ai-message is-'+role);b.append(el('strong','',role==='user'?'나':'Gemini'));const content=el('p','',text);b.append(content);log.append(b);return content;}
  if(!chain.length&&!busy){const welcome=el('div','general-chat-welcome');welcome.append(el('h3','general-chat-greeting',greeting()),el('p','','무엇을 도와드릴까요?'));const suggestions=selected.length?[['자료 핵심 요약','선택한 자료의 핵심 내용을 정리해 줘'],['추가 확인할 점','선택한 자료에서 판단을 위해 추가로 확인할 내용을 정리해 줘'],['다음 행동 정리','선택한 자료를 바탕으로 다음 행동을 함께 생각해 줘']]:suggestionSet;for(const [label,text] of suggestions)welcome.append(button('✦ '+label,()=>{draft=text;render();$('general-chat-question').focus({preventScroll:true});}));log.append(welcome);}
  let animateNode=null,animateText='';for(const t of chain){const q=bubble('user',t.question);if(t.reportIds.length)q.parentNode.append(el('small','','참고 보고서 '+t.reportIds.length+'개'));for(const f of t.files||[])q.parentNode.append(el('small','','첨부 · '+f.name));const answer=bubble('assistant',t.answer.replace(/\*\*(.+?)\*\*/g,'$1'));if(t.id===newAnswer){animateNode=answer;animateText=answer.textContent;}}
  if(busy&&pending){bubble('user',pending);const thinking=el('div','general-chat-thinking');thinking.setAttribute('role','status');thinking.append(el('span','','Gemini가 생각 중'));for(let i=0;i<3;i++)thinking.append(el('i'));log.append(thinking);}panel.append(log);
  const composer=el('div','insight-ai-composer'),inputShell=el('div','general-chat-input-shell'),chips=el('div','general-chat-files');for(const [i,f] of files.entries()){const chip=button('첨부 · '+f.name+' ×',()=>{capture();files.splice(i,1);render();});chip.disabled=busy;chips.append(chip);}inputShell.append(chips);
  const input=el('textarea');input.id='general-chat-question';input.rows=3;input.maxLength=4000;input.placeholder='무엇이든 물어보세요';input.setAttribute('aria-label','Gemini에게 질문');input.value=busy?'':draft;input.disabled=busy;const sendButton=button('',send);sendButton.className='general-chat-send';sendButton.setAttribute('aria-label','질문 보내기');sendButton.title='질문 보내기 · Enter';const icon=document.createElementNS('http://www.w3.org/2000/svg','svg');icon.setAttribute('viewBox','0 0 24 24');icon.setAttribute('aria-hidden','true');const path=document.createElementNS('http://www.w3.org/2000/svg','path');path.setAttribute('d','M12 19V5m-6 6 6-6 6 6');icon.append(path);sendButton.append(icon);sendButton.dataset.generalSend='';const update=()=>{draft=input.value;sendButton.disabled=busy||loading||!draft.trim()||configuration?.ready===false||configuration?.enabled===false;};input.oninput=update;input.onkeydown=e=>{if(e.key==='Enter'&&!e.shiftKey&&!e.isComposing){e.preventDefault();if(!sendButton.disabled)void send();}};
  inputShell.append(input);const controls=el('div','insight-ai-actions');const upload=el('input');upload.type='file';upload.multiple=true;upload.accept='.png,.jpg,.jpeg,.pdf,.txt,.md,.csv';upload.hidden=true;upload.onchange=()=>void attach(upload.files).catch(()=>{status='파일을 읽지 못했어요.';render();});const add=button('＋ 파일 첨부',()=>upload.click());add.disabled=busy;const models=configuration?.models?.length?configuration.models:[{id:'gemini-3.5-flash-lite',label:'Gemini 3.5 Flash-Lite · 빠른 대화'}];if(!models.some(m=>m.id===model))model=models[0].id;const modelSelect=button('',()=>{const menu=el('div','general-chat-model-menu');menu.setAttribute('popover','auto');menu.setAttribute('role','listbox');menu.setAttribute('aria-label','Gemini 모델 선택');for(const option of models){const item=button('',()=>{capture();model=option.id;menu.hidePopover();render();document.querySelector('.general-chat-model')?.focus({preventScroll:true});});item.setAttribute('role','option');item.setAttribute('aria-selected',String(option.id===model));item.dataset.model=option.id;item.append(el('span','',option.label),el('span','',option.id===model?'✓':''));menu.append(item);}menu.addEventListener('keydown',event=>{const items=[...menu.querySelectorAll('button')],i=items.indexOf(document.activeElement);if(['ArrowDown','ArrowUp','Home','End'].includes(event.key)){event.preventDefault();items[event.key==='Home'?0:event.key==='End'?items.length-1:(i+(event.key==='ArrowDown'?1:-1)+items.length)%items.length].focus();}});menu.addEventListener('toggle',event=>{modelSelect.setAttribute('aria-expanded',String(event.newState==='open'));if(event.newState==='closed')menu.remove();});panel.append(menu);menu.showPopover();const r=modelSelect.getBoundingClientRect(),width=Math.min(330,innerWidth-24);menu.style.width=width+'px';menu.style.left=Math.max(12,Math.min(r.left,innerWidth-width-12))+'px';menu.style.top=Math.max(12,r.top-menu.getBoundingClientRect().height-8)+'px';menu.querySelector('[aria-selected="true"]')?.focus({preventScroll:true});});modelSelect.className='general-chat-model';modelSelect.setAttribute('aria-label','Gemini 모델 선택');modelSelect.setAttribute('aria-haspopup','listbox');modelSelect.setAttribute('aria-expanded','false');modelSelect.disabled=busy||loading;modelSelect.append(el('span','',models.find(m=>m.id===model).label),el('span','','⌄'));controls.append(add,modelSelect,sendButton,upload);inputShell.append(controls);composer.append(inputShell);const state=el('p','insight-ai-chat-status',loading?'Gemini 연결과 기록을 확인하고 있어요…':status);state.setAttribute('role','status');composer.append(state);if(status&&!busy&&!loading)composer.append(button('연결·기록 다시 확인',load));composer.append(el('p','general-chat-help','Enter 전송 · Shift+Enter 줄바꿈 · 파일 3개 / 총 2MB'));composer.append(el('p','general-chat-help','질문과 선택한 첨부만 Gemini로 전송됩니다. 민감한 개인정보는 넣지 마세요.'));quota=quotas.find(q=>q?.model===model)||null;const usage=el('div','general-chat-quota');usage.append(el('span','',quota?'모아온 앱 한도 · 오늘 '+quota.remaining+' / '+quota.limit+'회 남음':'모아온 잔여 요청 · 확인 필요'));if(quota){const meter=el('progress');meter.max=quota.limit;meter.value=quota.remaining;meter.setAttribute('aria-label','오늘 남은 요청 수');usage.append(meter);}usage.append(el('small','','앱 사용 기록 기준 · 00시 초기화 · Google 실제 잔여량은 AI Studio에서 확인'));composer.append(usage);panel.append(composer);if(!panel.isConnected)document.querySelector('.app-area').append(panel);update();log.scrollTop=chain.length||busy?log.scrollHeight:0;if(focused&&!busy)input.focus({preventScroll:true});
  if(animateNode){newAnswer=null;if(!matchMedia('(prefers-reduced-motion: reduce)').matches){const letters=Array.from(animateText);let count=0;animateNode.textContent='';animateNode.classList.add('is-revealing');const step=()=>{if(!animateNode.isConnected)return;const follow=log.scrollHeight-log.scrollTop-log.clientHeight<60;count=Math.min(letters.length,count+Math.max(3,Math.ceil(letters.length/100)));animateNode.textContent=letters.slice(0,count).join('');if(follow)log.scrollTop=log.scrollHeight;if(count<letters.length)animation=requestAnimationFrame(step);else animateNode.classList.remove('is-revealing');};animation=requestAnimationFrame(step);}}
 }
 window.moaonGeneralChat=Object.freeze({open,close,clear,setReports:value=>{if(Array.isArray(value)){capture();reports=value;render();}}});
 new MutationObserver(()=>{const n=document.querySelector('.general-chat-greeting');if(n)n.textContent=greeting();}).observe(document.querySelector('#business-settings-open'),{attributes:true,childList:true,subtree:true});
 window.addEventListener('moaon-ai-keys-changed',e=>{if(e.detail?.provider==='GEMINI'){loaded=false;if(opened)void load();}});document.querySelector('.app-area').addEventListener('keydown',e=>{if(e.key==='Escape'&&opened&&!document.querySelector('dialog[open]')){e.preventDefault();close();}});render();
})();
